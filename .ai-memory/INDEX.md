# AI 记忆主索引（恢复入口）

> **作用**: 这是会话记忆的唯一入口。无论上下文被 compact 还是 clear，新会话只要从这里开始，就能恢复全部"原始记忆"和"固定开发管理方法"。
> **更新**: 2026-08-01
> **维护约定**: 任何新增/变更的长期记忆，都必须在本文件登记一行，否则视为未固化。

---

## 0. 恢复协议（compact / clear 后必做）

新会话或上下文被压缩/清空后，**按顺序**读以下文件即可恢复工作状态：

1. 本文件 `.ai-memory/INDEX.md` —— 全局地图
2. `.ai-memory/communication-preferences.md` —— 沟通约定（**始终中文**）
3. `docs/FUXI_WORKFLOW_CN.md` —— 固定开发管理方法（优先级/门禁/风险边界，必须服从）
4. 与当前任务相关的专题记忆（见第 2 节）
5. 最近进展：`git log --oneline -15` + `docs/DEVLOG.md` 尾部

> 原则：**不靠口头记忆，规则必须落在仓库文件里**。本索引就是那张地图。

---

## 1. 长期记忆文件（.ai-memory/）

| 文件 | 内容 | 状态 |
|---|---|---|
| `INDEX.md` | 本主索引 / 恢复入口 | 活跃 |
| `communication-preferences.md` | 语言（中文）、风格、高风险操作需确认 | 活跃 |
| `competition-chart-timeaxis.md` | **竞赛对比图时间轴/缩放/持仓浮窗(2026-07-29,已部署 2026-08-02 pid 711520 commit `e235edd` md5 `37b07ad1`)**。用户报的四个症状(1天3天7天30天没用、横轴不随周期变、曲线只有 7.31 以后、不能拖动缩放)源于**两处截断+一个轴类型**：①后端 `hours=0` 走 `GetLatest(500)`=最近 500 行≈7.5 天(claude 真实 44.4 天/3284 条),已加 `GetAllAscending()`；②前端在**合并四交易员之后**又 `slice(-500)` → 线上实测 3D/7D/30D/全部**四个按钮可见区间完全相同**(都是 07-31 01:23..08-03),已删客户端切尾、改服务端 `max_points`(默认 1500)；③X 轴是**类别轴+预格式化字符串** → 间距与真实时间无关(数据空洞看不出来)、标签取自按钮而非屏幕跳度、且类别轴**根本没有数值域可缩放** → 改数值时间轴。降采样必须**分桶取min/max保峰谷**(等间隔抽样会跳过尖峰 → 同一曲线在不同缩放级别给出不同最大回撤；线上已验证峰 299.61/谷 138.74 在 100000/1500/300 三档预算下与库中真值完全一致)。浮窗持仓额度**不新增字段**而用 `GetExposureAtTimes` 扇描线回放 `trader_positions`(新增列只覆盖未来、历史全空)；名义额度=开仓价×数量,**不是** mark 价(历史 mark 价没存),UI 已如实标注；`position_count`(交易所快照)与 `position_count_recon`(本地回放)**并列不合并** —— 不一致正是账本漂移的唯一信号(实测一致率 91.7/98.0/95.7/97.6%)。⚠️ 生产实测 **69.7%** 的分钟桶只含 1 个交易员 → 持仓字段必须沿用上次读数并带 `-Nm` 龄期标签(否则四行里三行永远空白,或把旧读数伪装成当前读数)；pnl 沿用但**不向前回填**(晚开始的交易员不能在存在之前就出现在图上)。**三个自纪**：`clampDomain` 最小跳度算了却只在撞边界时写回(中部缩放仍可塌缩成点)、`zoomDomain` 放行 NaN 锚点(`Math.min/max` 对 NaN 是传播不是夹取 → 整图空白)、公开免鉴权端点对 `traderManager==nil` 段错误。⚠️ **部署隔离教训**：线上 `ede8ea9`..dev 之间夹着 9 个回测 commit,而**回测包不是纯离线** —— `api/handler_blocksim.go` 把它链进生产二进制且 `startBlockSimReplayer()` 常驼,`realizedPnL()` 修正会改 blocksim 输出 → 必须建 `deploy/chart-only` 分支 cherry-pick,**部署前先跑 `git diff --name-only <线上commit>..HEAD`**。**踩坑**：构造 fixture 不能用 `sqlite3 .mode insert`(按位置插值,`AutoMigrate` 列序与生产库不同 → `entry_price` 灌进 `entry_time`,伪装成“回放全 0”的假 bug),必须按列名拷贝；jsdom 组件测试需 stub `ResizeObserver`。 | 活跃 |
| `claude-code-env.md` | 本机 CLI 配置：上下文窗口 ~570k、autoCompactWindow=500k、网关 failover | 活跃 |
| `production-deployment.md` | **2026-08-04 挂单量档位归属 + 动态武装记忆不被擦除上线 (pid 815160, commit `f997a6a`, md5 `34b3c1a1`)**。SOLUSDT long **2.5 小时 654 次 reclaim + 438 轮 degraded** 的**一条完整因果链**：①qty resize **误撤邻档 TP**（TP1/TP2 相距 0.1126% 落在 0.2% 容差内，TP1 自己成交后把 TP2 的单当成自己的覆盖）→ **价格容差只能识别"是不是同一档"，绝不能用来"区分相邻档"**，改 `nearestQtyTargetForOrders` 唯一归属；②重挂被 OKX `51279` 拒 → reconciler 写 `reconcile_failed:` **覆盖掉 `native_trailing_armed`**；③**死锁**：`protectionState` 是**内存 map**（不持久化）而账本持久化 —— 账本 armed 让武装路径判"已 armed 跳过"（`armedFingerprints` **只读账本、从不设内存状态**），内存 false 让 `classifyTrailing` **第一行 `if !o.Armed` 就 return false** → `staleTrail` → degraded → reclaim 重写账本 → 复读，**谁都不会去修对方**。这也解释了 `claimedTrail=1` 与 `staleTrail=1` **并存**（集合里有那张单，但没走到集合那一步）。⚠️ **诚实记录：churn 停下不是新加 rehydrate 的功劳**，`🩹` 至今 0 次 —— 重启后是**早已存在**的 `auto_trader.go:658-666` 从账本恢复的（连全平档优先的优先级都一样）；治本的是①②。⚠️ **既有风险(未动)**：`auto_trader.go:658-666` 只读账本不验交易所，陈旧 armed 记录会掩蔽 `missingTP`，下次碰这块优先修。⚠️ **两个被验证推翻的提议**：「`native_trailing` 纳入 store 独占组」**会破坏真实不变量**（多档 trailing 并存是正常态，会互相降级）；「`parts[4]`→`parts[5]`」**是死代码**（该函数只对 `break_even_stop` 可达，BE 指纹正好 5 字段）。基线→部署后：degraded **48→0**、SOL reclaim **48→0**、`protected verified=true` 432→441、ERRO/panic/missing/unexpected 全 0，5 持仓全 `protected verified=true`。门禁：22 包全绿 EXIT=0、14 新用例、**四处修复各做破坏性验证**（还原任一处都能让用例转红 —— 只看"测试通过"证明不了用例在钉不变量）。<br>**2026-08-03 保护档位角色感知匹配 + 计划闸门 + 结构位梯队上线 (pid 778013, commit `3d91258`, md5 `b10fe32b`)**。三个独立成因指向同一症状"保护单被误撤且不重挂"：①档位身份**只按价格**（0.2% 容差下 342 份快照 259 份至少一对档位互撞）→ `consumeAllowedProtectionSlot` 改角色+价格三级匹配；**前置条件是 Binance 适配器补 `ProtectionRole` 解码**，不补则改了匹配器也静默退回按价格匹配。②计划闸门 `filterCancelIDsAgainstPlan` 判据是**"撤掉会不会让某档失去覆盖"**而非"价位在计划里" —— 后者会让同价位重复单永久驻留、永不收敛。③**TP1 消失的真正机制与碰撞无关**：`validateProtectionPlanExecution` 在 reconcile 路径 `quiet=true`，滤掉档位**连日志都没有且不登记容忍名单** → 不在计划里（不重挂）+ 不在 allowed 里（判多余撤掉）= **永久真空**；现场证据是 `missingTP=false unexpectedTP=1` 两个数字同时成立。结构位梯队 T1 swing → T2 order block → T3 聚类（**前高前低+成交密集区在这层**）→ T4 bar 极值，斐波那契刻意排除；回看窗 28→120（T3 需要历史深度，28 根把这层饿死，是 ZEC 边界为空的一部分）；`EntryTolATR` 默认 0.25×ATR 让止损坐在结构外侧（压在位上等于把"价格触碰"和"结构失效"当同一件事）。⚠️ **隔离性教训不是一次性的**：`e235edd..dev` 又夹进同一批 9 个回测 commit，`go list -deps .` 仍命中 `trader/backtest` 且 `startBlockSimReplayer()` 常驻 → 每次部署都必须走 cherry-pick 分支并验证 `git diff --name-only <线上>..HEAD \| grep -c trader/backtest/` = 0。⚠️ **grep "401" 会误匹配 `auto_trader_loop.go:401` 行号**，不是鉴权失败。⚠️ 部署后三条新日志全 0 **不代表未生效** —— 闸门只在救单时打印，`🧱 Structural boundary` 只在**开仓时**触发，而当时唯一持仓是重启前开的。 | 活跃 |
| `unified-protection-system.md` | 统一保护系统全量记忆（**2026-08-04 `protectionState` 一格装五件事 = SOLUSDT 死锁的根因是数据结构不是逻辑（已实现待部署）**：每仓位**只有一格 string**却同载 ①武装进度(8 值) ②交易所校验 ③对账失败 ④回撤已触发 ⑤面板观测，**写 ②~⑤ 顺手擦掉 ①**；白名单式修法必然反复漏 —— **漏点数=写入点数(20 个)**，新增写入点就重漏一次且**无编译期/测试期信号**（那份手写并集真漏过 `managed_drawdown_armed`）。改**按维度分家**：武装存 `protectionState`、观测存新 `protectionObservation`、同一把锁；`setProtectionState` 仍收字符串（**20 调用点+83 测试断言零改**）但先判归属再只写一维，"写观测擦掉武装"从**每处提防**变**结构上不可能**；`getProtectionState` 仍返单值（武装优先）故读者与谓词零改；`drawdown_triggered*` 是唯一"观测且须清武装"语义（已平仓，指纹不再成立）；**删掉两个已冗余白名单**（`protectionStateAfterReconcileFailure` 整函数 + reconciler ExchangeVerified 跳过写入）—— 同一不变量两套机制会让后人分不清哪个承重。⚠️**反直觉细节**：CAS 比较基准**必须是合成值**，只比武装维度会让"武装空+观测 verified"（**线上最常见形态**）永远 CAS 不成立 = **从未武装的仓位再也武装不上**，比原 bug 更严重；回滚同理只还原武装维度。门禁：build/vet 干净、全仓 test 全绿、`-race` 无竞争、**11 新用例逐个破坏性验证**（整格覆盖→4 红、CAS 只比武装→2 红、回滚抹观测→1 红、clear 漏清观测→1 红），其中 **2 个注入下不失败是故意的**（防过度修正：观测必须保持可见，`giveback_guard` 依赖）。⚠️**未动既有风险**：`auto_trader.go:658-666` 启动恢复只读账本不验交易所，陈旧 armed 掩蔽 `missingTP`。⚠️**同根未解**：武装维度仍单值，表达不了"全平档+部分档同时武装"，`auto_trader_risk.go:3223` 的"只撤孤儿单"补丁仍是唯一防线，彻底解法是拆成全量/部分两个独立槽。<br>**v1.17.7 追查"交易所 DD 为什么被重新挂",答案是两件独立的事(2026-07-30,已部署 2026-08-02 pid 626448 md5 5acea85f 提交 `6a817f2`;零 churn=撤 0 挂 0,但**主修复点无实盘证据**,`status=activated` 只在已武装 DD 档打印而部署时 4 持仓全 arm pending,下次武装必须回看)**：①**07-31 22:23 真重挂**,起因是 OKX `51149「Order timed out」`是**半成功** —— 单子其实落地(algoId `3791832449865392128`),响应体丢了;我们当硬失败丢弃、不采集 algoId、**从不按 `algoClOrdId` 回读**(`grep -rn "51149"` 全仓零命中,4 天 6 次下单失败全是这个码),账本无 orderID 便无法认领,10 秒后正确补挂 `3791832803965304832`;那张超时单是 **OKX 自己**几秒后撤的(我们无撤单日志)。落地记录由 `order_sync.recordProtectionOrder` **静默**写入 DB(只在失败时打日志)= 一直没被发现的原因。②**08-02 02:45 什么都没丢,是判据读错了地方**:OKX 适配器把 trailing 激活状态从**哪个查询桶回了这一行**推断(`""` 桶先跑 + `seenTrailingIDs` 去重掉第二轮),**从不读行自己的 `state`/`moveTriggerPx`** —— 桶不是订单的属性;叠加 `nativeTrailingEffective` 的幽灵规则"mark 越过 activePx 且交易所说未激活",而**已激活的跟踪单必然早已越过 activePx** → 对健康单**逻辑反转**。证据:OKX 4 天 `pending_activation` **1528** 次 vs `activated` **5** 次;全部 **1104** 次 "NOT effective" 穿越幅度 0.20%–1.94%(中位 0.52%)= **每次都刚好落在 0.2% 容差外**,无一例死单特征。Binance 免疫(`activated` 标记可靠)。→ 熔断 → `*_exchange_failed_armed` 是**单向锁**(`drawdown_order_reclaim.go` 改写账本但**零处 `setProtectionState`**),面板假报 ~410 次,同一轮 reconciler 还在打 `verified=true coverage is complete`。修法三件:trailing 结构体补 `State` + 激活改读行自身;歧义失败按 `algoClOrdId` 回读认领(**必须显式比对 clientID**,某些 OKX 端点忽略未知过滤器回全量 → 会认走别档的单);覆盖恢复时把面板降级回 **managed 家族**(写 native 会擦掉 monitor 武装记忆导致每轮重 arm)。**只改判据与回读,不撤任何交易所挂单**。**我订正的猜测**:曾假设 OKX 拒绝激活是因 reduce-only 数量超过缩水后持仓 —— 114 次熔断有 9 次在**永不超量的 30% 档**上,**撤回**。**待处理(高风险)**:部分平仓后档位量漂移(ETH dd2 0.047/0.064=73%,设计 30%),修它要撤后重挂在场单。 / **v1.17.6 全量日志合规审计:64 笔平仓全合规,顺带挖出 4 个缺陷,其中 3 个的共同形态是"保护实际在位,账本和日志在说谎"(2026-07-31,待部署)**：①一张活单两个主人 —— ATR 解析后 BE1/BE2 挂单价相对间距 **0.1503% < protectionPriceTolerancePct(0.2%)**,而 `matchingBreakEvenOrderID` **只按价格匹配** → BE2 认领了 BE1 的单;叠加 reconciler 用生配置、monitor 用 ATR 解析后规则 → **同一张单 3 个竞争身份**,而 "current 胜" 在两个 writer 交替写入时**永不收敛**(实测翻转 53 次)。修法:档位抑制(先到者赢)+ 两管线同源 + **与写入顺序无关**的字典序裁决 + 存量记录退役,**不撤任何挂单**。抑制点必须在 `replaceStaleBreakEvenTierOrders` **上游** —— 替换容差 0.05% 比匹配容差 0.2% 更紧,0.1503% 正好落在两者之间 = 潜伏的挂撤循环(24h 零次替换,从未发作)。②reclaim 分支裸 `return` 带着 ExchangeVerified=false + 空 Summary 回去 → 全天 **7688 条** "not verified" 假警告、BTCUSDT LONG 连续 **1900 轮**假 degraded,而 missingSL 全程 false、撤单数 0;与容忍分支是**同一个顺序问题**(判决必须在 🧭 日志前做完),上次只修了容忍分支。顺带发现两处**各自内联**同一布尔式(注释写"同源"其实不同源)→ 收口成唯一判据 `protectionCoverageComplete`;错判方向**不对称**:该 false 判 true 是真缺保护却标已验证,所以 SKHYNIXUSDT 那 58 轮 missing profit owner 必须继续未验证。③time-stop/max-hold 在 NO_POSITION 上打 `✅ closed`(**纯可观测性,幂等未坏**,但按该行计数会把平仓数**数成 2 倍**)→ 新增 `closePositionByReasonWithOutcome`,老签名行为一字不变。④OKX **51277**(空头 TP 触发价高于最新价)的档位被计入 tier 失败 → 2 次重试全败 → ❌ reconcile failed,而其余 3 档 TP + SL **都挂上了**,且该档 2 秒后就成交 —— 它本来就在成交。修法:typed sentinel + 记为跳过,**用显式错误码白名单而非模糊字符串匹配**(误判成可跳过会静默吞掉真实挂单失败),SL 侧永不跳过。**我犯的两个错**:拿 claude 的 1h 配置核对 Claude-R(primary=15m)的持仓 —— `AlignToPrimaryTimeframe` 会把 `ATRProtection.Timeframe` 覆写成 primary,**静态配置显示 atr_tf=1h 是误导**;以及**把巧合当因果** —— 一度报 51277 根因是 BE/TP 撞容差带,实为触发价已被越过,`63177.3×0.995=62861.4135` 与 BE1 相等纯属巧合。**自查测出的真 bug**:畸形指纹时 stage 为空串,`"" != "BE1"` 被误判成"跨档",而字典序在畸形串上无语义(`"garbage" > "*|*|0.9638|..."` 会留下畸形记录、退役真档位)→ 加"两侧 stage 必须都解析得出"。确定性裁决**只限 break_even_stop 且只限 stage 不同**:同 stage 不同身份是同档重新解析(必须 current 胜),trailing 那一路只有一个 writer,字典序会留错人 —— 既存测试 `TestSupersede_OneLiveOrderCannotHaveTwoTierOwners` 就是被我第一版过宽的改动搞红的。验证:23 包 ok / 0 FAIL,`-race` 无竞态**** / ****v1.17.5 棘轮"亏损中也锁紧" —— 文档与 UI 都写 Default 1.0,线上实际是 0(2026-07-30)**：起因是用户问「近两天棘轮锁紧次数/币种/效果/后续走势」而非报 bug；`TrailMinProfitATR` 是 `float64`+`omitempty` → **"未设置"与"显式 0"不可区分**，5 个策略全未设置 → 归一化读回 0 = 闸门关闭，而注释写 "Default 1.0"、UI 渲染 `?? 1.0`（文档和界面都在说谎）；加上 `TrailOnLoss` 默认 true → ETHUSDT LONG **00:23 开仓 00:24 就棘轮**（触发价 1921.15 对入场 1921.21，毫无盈利）。**我第一版把"棘轮"整体判为有害是错的**：`structural_sl` 天生 0 胜率（97/97 全亏、其中 **89 笔根本没棘轮**），且棘轮组每笔亏得更**轻**（-1.68% vs 静态 -2.01%）、控时长横比胜率更高（63% vs 57%）、置换检验 **p=0.312 不显著**；站得住的判据是**逻辑不是统计** —— 亏损中锁紧在定义上锁不住任何利润，只把失效判定从"结构破了"挪进噪音带。因果证据：3 笔有 K 线覆盖的亏损中棘轮，**不棘轮的入场结构位全部从未被击穿**（这 3 次止损只因棘轮而存在），止损后反向 +2.36%/+2.48%/+4.72%，实亏 -9.31 USDT；**天然对照**=两笔 WLD SHORT 相隔 2 分钟开仓，收紧 2.01% 的被打掉 -3.40、收紧 0.15% 的活过同一根冲高后 +0.47 出场。修法：`*float64`（nil→1.0，**显式 0 仍关闭闸门**，因 UI `min="0"` 让 0 是合法值，不能用负数哨兵）+ `TrailOnLoss` 默认 false + `TrailMinProfitATRValue()`；**无需写生产库**（两字段全未设置，归一化结果从不回写 DB，已验证幂等）。顺带挖出 `backtest/config_baseline.go` **手抄**了一份 nil 默认判断 → 线上改默认回测不跟（**手抄默认值就是漂移来源**）。反向验证：默认值改回旧值 → 6 例测试转红；回放 8 次真实事件 → 拦 7 放 1（放行的是实际 +2.37 那笔），XAU 顺向 0.96ATR **差 0.04 被拦**** / ****v1.17.3 被账本"屏蔽"的委托泄漏 —— 危害在交易所侧而账本侧看起来完全干净(2026-07-29,提交 `17e0f88`+`1edc4f6`,已提交未部署)**:线上 ETHUSDT 空头同时挂 **4 张 trailing、其中 3 张全平**、回调率各不相同,而协调器**永远报 `staleTrail=0`**;危害不在账本(账本自洽)而在交易所 —— 谁先触发谁平掉**整个仓位**,泄漏出来那张回调**最紧**的单会比任何在用档位设计的时机都更早打掉仓位,用户看到"莫名被提前平仓"却查不出账本问题;根因是 v1.16.15 留的尾巴:`drawdownRuleIdentity` 抹掉 entry/qty 但**保留 ATR 解析后的 min/max 百分比**,ATR 重新冻结或倍数被改(此处 0.9→1.0→1.5 ATR)时同一档位算出不同身份、旧记录继续 armed,而 `classifyProtectionOrder` 只要 `trailingOwnership` 认得 orderID 就标 `expectedDynamicOwner` → **泄漏的账本记录成了泄漏委托的保护伞**,stale 清理一辈子看不见;修法用**基数不变量**(已配置档位数 < 活跃且被认领的 trailing 单数 → 逐张回看有无**当前**档位认它,没有则标 superseded 交回既有清理路径),**我第一版写"认领比例之和>100% 就收敛"被自己的测试打回**(`claimed exposure still 130.0%` —— 100% 全平+30% 部分**本来就是正常两档**);两道基数闸门取证不足不动手、全部对得上时**不猜**而报 ERROR 保留认领;顺序必须在 `resolveConflictingTrailingClaims` **之后**(先收敛"一张单多档",敞口计数才不会重复计数同一张单);**最值得记的反向验证**:删掉两道基数闸门后**测试全绿**=闸门无覆盖,补 `ATRReresolutionWindowMustNotUnshieldTheOnlyLiveOrder` 才转红;9 例测试用线上真实数值(入场 1890.56);顺带补观测缺口 `1edc4f6`(`T<N>` 只在 clOrdId/clientAlgoId 里、放单后日志不打印 → OKX+Binance 成功日志各补一字段),并**订正我上一轮部署记录**:那里写"冲突按交易所形状解决",日志普查显示实际判据是"是否属于该仓位当前档位集合"** / ****v1.17.2 v1.17.1 的档位标识在 ATR 单位策略上是死代码,以及它撑出的"一单多主"账本腐烂(2026-07-29,4 处修复+17 例测试)**:`drawdownTierTagIndex` 拿**已 ATR 解析**的 rule(min=3.6791)去比 config 里的**原始倍数**(`min_profit_pct: 2.5, unit: atr`),判据是精确浮点相等 → 恒返回 0 → 5 个策略里 4 个(BN/Claude15/GPT-ct50/claude-ct30)全是 ATR 单位,**部署当天线上没有一张单带上过 `T<N>`**;修法=序号定义改成"在该仓位当前生效的**已解析**规则集里的位次",新增 `canonicalDrawdownTierRules` 作唯一基准供挂单侧+认领侧共用(顺带让 AI 规则也能带标识,并用三键规范排序防同 MinProfitPct 两档位次漂移),浮点相等换 1e-9 绝对容差(同输入不同运算顺序会差 1 ULP,精确相等又回到恒 0);**更深的那个**:`reclaimLiveDrawdownTrailingOrders` 是 `getActiveDrawdownRulesForPosition` **唯一**没跟着调 `resolveDrawdownRulesATR` 的消费者(risk/decision/reconciler/execution 四处都有),把 2.5 ATR 当 2.5% 算出 activation 0.296010 而真单是 0.292060(漂 1.33%,形状匹配 1% 容差刚好卡不住)→ 退到 "activated 短路+取最低未用档"把两张单**认反**,且落库 fingerprint 带 min=2.5000 与 arm 侧 min=3.6791 在 `drawdownRuleIdentity` 看来"不是同一档" → `supersedeOlderArmedRecords` 窄匹配永远碰不到 → BN WLDUSDT 4 条 armed 记录抢 2 张单、每 20s 重写;新增"一张活单只能有一条 armed 记录认领它"不变量(`trader/drawdown_claim_conflict.go`,挂在 reconcile 每轮,手里已有 openOrders 不发额外请求)——**我自己踩的坑**:第一版放在启动加载、判据"保留 UpdatedAt 最新",查线上真账本才发现**错的那两条恰恰更新**(坏 reclaim 每 20s 重写它们),按取最新清扫会留下错的、比不清扫更糟,整段删掉重做成三级判据(① 形状对得上交易所真单 ② 档位在当前规则集里 ③ 才按时序并打 WARN 自证是猜的),放 reconcile 就是因为启动时唯一可用的判据恰好是错的那个;已激活单弃权形状判定(StopPrice 变移动跟踪价、Binance 不报 callback,会把**正确**记录判成错的);只管跨档冲突,同档陈旧分叉仍归时序路径(有反向锁);`tierTaggedRuleIndex` 改直接用序号当下标后加"能查就查"的形状交叉验证,挡住"挂单后策略档位表被改/混合单位解析重排"导致的认错档;**教训**:v1.17.1 那 13 例测试全用**百分比**规则构造 fixture,而 4/5 实盘策略是 ATR 单位——测的是实盘根本不走的分支,凡"配置项会被运行时解析器改写"的功能必须至少一例走**解析后**的值** / **v1.17.1 挂单自带档位标识=多档 DD 第二重身份(2026-07-29,提交 `8133f80`,已随 `90de970` 部署但在 ATR 策略上从未生效)**:第一重身份是持久化 ExchangeOrderID(三个 matcher+面板早已 ID-first),但它单点在"记录必须写成功且还在"——persist 失败/交易所不回 orderID/记录被清都会让活单在交易所侧变无主,只能按 qty+激活价+回调率猜档而 place-at-open 后多档并存必然碰撞;修法=档位序号编进 clOrdId/algoClOrdId(`<brokerPrefix><code2>["T"<1-9>]<nonce>`,≤32 字符,**"T" 作分隔符靠 nonce 是 hex 保证无歧义**,旧 id 照旧解得出机制),档位走 reason 后缀 `native_trailing#2` 而非新增参数(reason 已流经 10 个适配器签名,`normalizeReason` 是所有机制比较的唯一收口点,在那剥掉 `#N` 则 OKX 定向撤单 wantReason/归因/CodeForReason 全不动——**这条等价性必须测试锁死,否则定向撤单整段跳过或归因断链**),broker 前缀私有于适配器包故解码留在各包、结果走新字段 `OpenOrder.ProtectionTier`;序号只作兜底识别绝不定义档位语义(位次随配置改动而变,语义仍归 fingerprint 三元组);顺手修掉 reclaim 对 activated 单一律形状匹配→退回"取最低未用档"把 dd2(close=100%)认给 dd1(close=30%) 的错认,改两级优先(先信自报档位,`Tier=0` 旧单才退形状匹配);**反向验证**禁用档位优先后判别性测试立刻红并复现该错认;13 例测试含两条反向锁——旧单必须保留形状认领(否则线上现存挂单全变可撤=最坏退化)、两单自报同档不得双重认领;**坑**:`trader/types/interface.go` 加字段引起 tag 对齐漂移而该文件 HEAD 本是 gofmt 干净的必须 `gofmt -w` 回去,仓库其余未格式化文件是基线别顺手动** / **v1.17.0 DD 不在交易所 + managed 陪跑形同虚设,6 根因一次性收口(2026-07-29,已部署 pid 2696350)**:RC-1 假成交(仓位缩水推断成交,全平档被 ladder_tp 一半以上就伪造出成交→永久拒绝重挂,8 次实例)改按 position_close_events 归属判定 / RC-2 managed 记录 `return nil` 压制交易所补挂,把双保险反转成接管 / RC-3 熔断永久闩死(复位需覆盖、覆盖需一张永挂不出的单)改 30min 冷却衰减 / RC-4 归属账本翻转让开仓挂好的活单落进 stale-duplicate 名单被撤(跳闸后 14 秒撤掉 algoId 3781523903370133504),改为重写账本去匹配交易所、绝不撤活单 / RC-5 幻影覆盖(armed 只是 DB 布尔量,trail=0 配 claimedTrail=1 抹掉 missingTP)改要求实盘活单背书 / RC-6 武装门禁 `cur >= min` 语义与移动止盈相反,改 `max(cur,peak)`,两条路径必须同改;**彻查副产品**:DD 分配表是全系统唯一"内存态状态机+只看当前值门禁"的组合,而 peakPnLCache 是持久化的,这个不对称是重启失忆的根;**踩坑**:我自写的 `clearReArmBreakerIfNoLiveOrder` 每轮归零等于废掉断路器(已删),以及 Go 把文件名 `_arm` 当 GOARCH 约束**静默跳过整个测试文件**(build/vet 都不报错、test 显示 PASS 但从未执行);**原"SKHYNIX/DD 无委托不危险"澄清段已作废**——不能用"这笔没亏钱"评价保护,只能看多档 DD 此刻是否真挂在交易所** / v1.16.27 休眠风险专项(2026-07-28,提交 `92a17f6`):三个"当前恰好成立但没有保证"的前提——①ProtectionCapabilities 10 个裸 bool 位置初始化,往中间插字段会静默把每个交易所后续标志位右移且照样编译通过照样跑过既有测试(反向对照实证:移位版 go build 干净,只有新测试抓到 lighter 被翻成支持部分平仓)→全改键名 ②"交易所是否回报 trailing callbackRate"只活在匹配器注释里+靠 `order.CallbackRate<=0` 值嗅探反推,而值嗅探原理上分不清"这个交易所不回报"和"这单真的是 0"→变成数据 ReportsTrailingCallbackRate(binance=false 因 algo 单列表无此字段/okx+bitget=true/未验证交易所保守默认 false 让不变量失败在安全侧) ③空 ExchangeOrderID 的 armed 记录在不能模糊匹配的交易所上不可恢复=v1.16.8 那 174 张 HYPEUSDT 孤儿单的**成因侧**(之前只清现场没堵入口):无 ID 又无 callbackRate 判别→每轮判缺失每个冷却窗口重挂一张无上界;更阴的是 supersedeOlderArmedRecords 只在 exchangeOrderID!="" 时才跑→空 ID 记录会把上一条已撤销的旧 ID 留成权威答案主动误导匹配器;修法=Binance setTrailingStopLossCore 原能返回 ("",nil)(下单成功但 AlgoId==0 拿不到句柄)现改为先撤单避免孤儿再返回 error(**不可管理的单比没有单更糟**)+新增命名不变量 trailingRecordIsUnrecoverable 收口四条 arm 分支而非各自自觉(空白字符视同为空;okx/bitget 空 ID 路径不受影响因能模糊匹配,反向对照验了去掉 venue 门会误伤 okx),兜底一律落 applyExchangeFailedLocalMonitor 本地 managed 陪跑不接管;**教训**:三者与前 13 次实例同属一个母题——"前提当前成立"≠"前提被保证",位置初始化赌没人插字段/注释赌有人看见并相信/空 ID 赌交易所刚好能模糊匹配,而让赌注变错的动作(插字段/改交易所/换适配器)全是日常动作,修法统一为把赌注变成不变量:能力变数据、判定收口成命名函数、失败默认落安全侧** /churn/回吐护栏/幻影 trailing v1.14/无激活价危险单+re-arm断路器 v1.15/place-at-open 统一保护:开仓挂全档+运行时补丢档+越过 activePx 用 activePx=0 立即重挂(v1.16.0 近价锚定被 OKX tick 取整坑→v1.16.1 热修)+profit-aware 判别/v1.16.2 多档匹配冲突根治:全委托单统一用 orderID 认单/v1.16.3 全档 churn 根治:全档 arm 补写 nativeTrailingArmTime,修 v1.16.2 引入的 dd1 无限重挂堆到 OKX 55 单上限/v1.16.4 面板红+周期重挂根治:同持仓存多条同 ruleFP armed 记录(部分平仓各留一条,身份校验只比 entry 不比 qty),map 随机序取到死单 ID → 改取 UpdatedAt 最大;300s cooldown 保留/v1.16.5 collapse 误撤兄弟档:全档迁移撤光该方向所有 trailing 单(含 4 秒前刚挂的 partial)→ 跳过被其他 armed 记录认领的单/v1.16.6 同 ruleFP 旧 armed 记录标 superseded;并记录两条"不能做"的边界:一刀切清 stale 记录会拉低防降档下限(alloc 只在内存,靠 DB armed 记录跨重启恢复)、"OKX 有僵尸单"已被 cmd/zombieaudit 证伪(orphan=0)/**v1.16.7 该 bug 类第 4 次实例:Binance/Bitget 部分平仓档 arm 分支 + OKX 未打 tag 兜底分支拿到 orderID 却不落库不写 armTime,而 300s cooldown 检查嵌在"已有 armed 记录"分支内 → 没记录=cooldown 永不生效 → 每 20s 重挂,HYPEUSDT 挂单 360 张/小时无上限增长;三处一起补齐+表驱动测试覆盖三交易所** /**v1.16.8 构造交易所对等测试项目(trader/venue_trailing_parity_test.go,4测试×okx/binance,均反向验证)挖出 HEAD 既存缺陷=该 bug 类第5次实例:findPartialTrailingReplacementCandidate 无阈值贪心匹配(b5a7aca 时同侧只挂1张,place-at-open 后 dd1 与局部档并存前提失效)→OKX 局部档 arm 时把 dd1 全平单当"自己漂移了"无条件撤掉;修法=复用 claimedTrailingOrderIDsForPosition 排除已认领单,不加新阈值;并把 300s cooldown 从"已有 armed 记录"分支内上提为无条件限频(armedFingerprints 不含 managed_drawdown→原分支永不可达);同时订正两处我自己的错误结论:①Binance open-algo 不报 callbackRate/activatePrice,适配器拿 triggerPrice 顶替且硬编码 activated→我审计报的活动价是移动止损位,用户读数 60.598 才对 ②审计工具漏调 SetExecutionPreferences(线上默认 preferUSDC=true)→查 SOLUSDT 而真实市场是 SOLUSDC→返回空集不报错→谎报"仓位裸奔"假警报,已修 zombieaudit/orphanlist;HYPEUSDT 仓位已由 break_even_stop 平掉并顺带清空 174 张孤儿单,原计划的不可逆批量撤单未执行也不再需要** /**v1.16.9 该 bug 类第 6 次实例(2026-07-27,用户从面板发现"两档配置却显示三档 0.6/1.2/1.8"):applyNativeProtectionTargetsAfterOpen(protection_execution.go)全文零次 ATR 调用,把 ATR 倍数当百分数直接挂单——另外三条路径(运行时 risk.go:204/展示 decision.go:1223/对账器 reconciler.go:476)都换算过,0efe033 修了展示、26104e6 修了对账器,a56f741 引入的 place-at-open 从没修;误差无固定方向(SOL 1.8ATR 实为 0.911%→太宽 2 倍;KAITO 1.2ATR 实为 3.44%→太窄 1/3);**第二个独立成因**:开仓传的 entry 是 marketData.CurrentPrice=下单前快照价(SOL 76.36 vs 实际 76.28),而 entry 既是 ruleFP 首字段又是 ATR→% 的分母 → 单修换算仍分裂成两族;附带第三后果:未换算档因 getCumulativeCloseRatioByRule 按 MinProfitPct 匹配不上、回退取最高档号累加 → 30% 的档挂成整仓;修法=挂单前调 resolveDrawdownRulesATR + 新增 syncRequestEntryPriceToExchange(用交易所实际持仓 entry 校正,偏离>2% 保留原值,纯 best-effort 不阻塞开仓);测试 trader/postopen_drawdown_atr_test.go 3×2venue 全反向验证;**并订正"OKX 看起来没问题"**:OKX 同样中招(KAITO/SPCX/WLD 都是原始+已换算成对 armed),只因 OKX 上报 callbackRate、面板显示已换算值把原始那条掩住,Binance 不回 callbackRate 才暴露三条并列 → 教训:venue 上报差异会掩盖同一缺陷,"另一交易所看着正常"不是缺陷不存在的证据;遗留待确认:7 个持仓存量多余挂单未清理(高风险)、9 条 6 月僵尸记录(trader 4801de05_..._1781597131 已不在 traders 表)、v1.16.8+v1.16.9 均未提交** /**v1.16.10 该 bug 类第 7 次实例(2026-07-27,清理 SOL 原始倍数单后 dd1 再没补挂):换算后的 dd1 指纹与被撤的原始倍数单不同 → 无 stored orderID → 落到兜底匹配"本方向第一张 trailing 单就是我"(place-at-open 之前的假设,那时同侧只可能一张),抓走 dd2 的 1.32 局部单;Binance 对每张 trailing 单硬报 activated → applyNativeTrailingDrawdown:2400 静默 return true,什么都没挂,活仓无 dd1 全平保护而日志+reconciler 都声称已覆盖;修法=findExistingFullTrailingOrder 与 hasMatchingNativeTrailingOrderForRule 两处兜底循环都用 claimedTrailingOrderIDsForPosition 跳过兄弟档已认领的 orderID(同 v1.16.8 招式,不加新阈值),兜底本身保留给真正无人认领的孤儿单;测试 TestFullTierFallbackSkipsSiblingClaimedOrder(线上真实数据+3 子例含回归护栏);上线验证 pid 2274258:SOL dd1 已补挂 2000001312879648(4.41)、ETH 部分成交后 dd1 按新仓位 0.081 正确重挂;并记一条排查陷阱:刚 arm 完的档位会连打 300s 的 🟠 arm cooldown,那是 v1.16.6 结构性兜底(cooldown 检查故意排在 armedFingerprints 分支之前),不是匹配失败** /**v1.16.11 该 bug 类第 8 次实例(2026-07-27,ATR 策略下 30% 部分止盈档按整仓挂单:WLD 应 114 挂了 380、CL 应 0.42 挂了 1.4,回调更紧会先触发→整仓平掉 runner 全没):根子在 resolveDrawdownRulesWithModes 每档从 result:=base(原始 config)起算、只在 mode=ai 时才采纳调用方值 → 全 manual 策略把调用方已 ATR 解析的规则整体还原成裸倍数 → 档位分配表把裸 ATR 倍数 3.0 存进语义为百分比的 MinProfitPct(日志 peak_trigger=3.00% 真值 4.2012%,updateDrawdownTierStates 也会提前转 tracking)→ getCumulativeCloseRatioByRule 精确匹配失败 → 兜底「取 min≤本规则的最高档」认领 dd1 累加其 100 并 clamp → close=30 算出 close=100;SOL/ETH 侥幸正常只因其解析后部分档 min 低于裸 3.0、兜底一条都没匹配上,"部分档 min 反而高于 dd1"就是本 bug 的外部指纹;修法=initDrawdownTiersForPosition 增 entryPrice 并在 mode 合并之后再跑 resolveDrawdownRulesATR(分配表恒为百分比)+ 身份匹配改用跨解析不变的 (StageName,CloseRatioPct) + 删掉会往上认领的数值兜底(匹配不到就返回本档比例,少保护一档远好过整仓被平,真实阶梯累加语义保留并有测试钉住);测试 trader/drawdown_tier_alloc_atr_test.go 3 例(claude-ct30 线上配置),反向验证报错即线上数字 ENTIRE position (380 units) instead of 114** 等) | 活跃 **线上 v1.16.21(2026-07-28 03:04 pid 2413205 md5 f4fe200a,含 v1.16.19+20+21,提交 `5cef90d`,回滚备份 `nofx.bak_v11621_20260727_185500`=v1.16.18 md5 e6abee65);上一版 v1.16.18(2026-07-27 23:40 pid 2381838 md5 e6abee65,含 v1.16.17+18);上一版 v1.16.16(pid 2339558 md5 4820ddd8,含 v1.16.14+15+16,回滚备份 nofx.bak_v11613_20260727_125039;v1.16.17+v1.16.18 已于 2026-07-27 23:40 部署 pid 2381838 md5 e6abee65 提交 5f2163f,回滚备份 nofx.bak_v11616_20260727_153756)**;上一版 **v1.16.13(pid 2298513 md5 b92d92da,提交 b18ff54);部署后核对:ETH/CL/WLD 从 1 tiers 变 2 tiers 数量正确、partial_profit_lock 命名正常、KAITO 单档确认是策略只配一档**;v1.16.11 上线验证:分配表日志已恒为百分比且与 arm 指纹一致(KAITO 8.60%↔8.5997、SPCX 3.29%↔3.2933、WLD 4.20% 原报 3.00%、CL 2.71%/2.43%),WLD 局部档已按 114 重挂(原 380)、CL 按 0.4 重挂(原 1.4),dd1 全平档仍 380/1.4;两张错量单 3778506109091213312/3778585660408365056 已撤;**v1.16.12 已编码待部署 md5 6d90f3c6**(该 bug 类第 9 次实例:computeDrawdownTierAllocations 的 allocatedPct 预算裁剪假设各档互斥,而 dd1 触发点 4.20% 低于部分档 5.60% 排序在前吃满 100 → 部分档 ratioPct=0 被 continue **整条丢出分配表**,线上日志"1 tiers"就是证据;后果:累加匹配不到/高水位不跟踪/managed 兜底永不执行该档/面板少一档;修法五处配套 = 全平档拿整仓且不占预算 + 累加时排除并存全平档(**关键陷阱**:档位加回来会让身份匹配成功,若仍累加全平档的 100 则 #8 从匹配路径复活,反向验证实测 cumulative=100% 1.40 vs 0.42) + supersede 循环禁止部分档注销全平档 + findRuleForTier 先按 StageName 身份匹配(TierIndex 指向内部排序切片,与调用方未排序切片错配) + resolveDrawdownRulesWithModes 空名改语义推断(原盖成 T2,arm 路径叫 partial_profit_lock,同一档两个名字);测试 4 例全反向验证,trader 全包 34.6s 绿;**教训:兜底 ⚠️ 日志即使结果正确也必须追因 —— 这次正确值完全来自 v1.16.11 兜底的保守方向,不是系统正常工作**);**v1.16.13 已提交待部署 md5 b92d92da 提交 b18ff54**(按用户"不行就 managed 一直陪跑,双保险,别搞接管"定性重构:原主循环 `if nativeTrailingHandled { continue }` 是**账户级接管**,managed 当轮全停摆;最严重后果=`accountReArmBreaker` tripped 分支谎报 allCovered 并把档位"交给"`applyExchangeFailedLocalMonitor`,而后者对 close>=100 的档**不下单也不注册执行器**,只写保护状态 → 交易所无单+managed 无执行器+面板 armed+回吐熔断器因 positionHasArmedProtection 放过回撤盈利仓,且 resetReArmFail 只在验证到覆盖时清零而覆盖需要一张永不下单的单 → **该仓位终身零保护**;改法 5 处+1 处回吐守卫 = 去掉账户级 continue(managed 始终评估,执行权只由每档 exchangeSideCoversDrawdownTier 判定,查有效性且读不到状态时 fail open)+ 门禁抑制时回滚 evaluateDrawdownTiers 已写的 executed(该函数先改状态再返回,不回滚则档位被永久过滤且 hasAllTiersCompleted 误判全退出)+ tripped 档覆盖判定直接 false + tripped 分支不再谎报覆盖也不重复补挂(对部分档会抖动)+ 无 allocs 老路径改查交易所单有效性不再信 armed DB 记录 + positionHasArmedProtection 对 *_exchange_failed_armed 要求存在档位分配;测试 E8/E9/E10 三端钉住,TestReArmBreaker_TripsAfter3Fails 按新语义更新;**教训:"双保险"vs"接管"在代码里的区别是闸门粒度 —— 账户级/仓位级 continue 一定是接管,只有下沉到"这一档此刻交易所侧是否真有效"并 fail open 才是陪跑**);**v1.16.14 该 bug 类第 10 次实例,已提交待部署 md5 ab4f4e81 提交 b4f35e0**(用户报"binance 又挂错了,有三单,比例好像还是固定百分比不是 atr";**先订正我自己的误判**:我用交易所上报的 stop 反推 callback 得出"裸 ATR 倍数"是错的——三张单都 activated,stop 是移动中的跟踪价不可反推,改从 DB ruleFP 核对确认 Binance ATR 换算**正确**,第三张是漏单;不变式=**每张挂在交易所的 trailing 单必须有且只有一个"已落库"的归属,匹配器只能认领它能证明属于自己的单**;档位有落库归属,开仓那张 50% immediate trailing 没有(单号只活在内存 map immediateTrailingIDs)→ 两个后果:①漏单:重启后 ID 丢失,cancelImmediateTrailing 空 ID 直接 return,单子永久留在交易所 ②误认领:进不了 claimed 集合,全平档位置兜底("本侧第一张未认领 trailing 就是我的")把 50% 单当整仓保护上报已覆盖,**实际只护住一半仓位**;加仓累加机制=applyNativeTrailingDrawdown 在"已覆盖"时从四个提前分支 return true 走不到末尾撤单,加仓时每档都已覆盖 ⇒ 每次加仓新挂一张又不撤旧的;实况 Binance BN CLUSDT 2.43+0.73+1.22、OKX ETHUSDT 3779472770300542976 qty 0.120/0.239 在 blob 原文出现 0 次;**OKX 无法靠 tag 识别(独立缺陷,已记录未修)**:okxReasonTag 拼 "<tag>_<reason>" 后截断到 16 字符而 okxTag 本身已占满 16 → reason 永远被截掉,每张 OKX 保护单 tag 完全相同,protectionReasonFromTag 永不命中,也劣化平仓归因;修法 7 项 = 新增 immediate_trailing_owner.go 归属注册表 + place 落库/cancel 回退到落库归属(重启可撤) + claimedTrailingOrderIDsForPosition 并入 immediate 单号(**一处修好四个消费者**) + 全平档兜底加覆盖率闸门(<95% 不认领,数量未知时跳过,不动按单号命中的路径) + **撤单点收口到 applyNativeTrailingDrawdown 的返回值**(原先散在六处且六处都只覆盖"刚挂上";放在这一层同时覆盖开仓与运行时 arm 轮询,让重启后只剩落库归属的漏单自愈回收) + 撤单失败按"交易所还看不看得见"分流(可见→重试/不可见→退休/查询失败→按未知绝不退休,**不看错误文本**) + 补 getPositionDetailsForFingerprint 的 at.trader 判空;测试 I1-I5 + 加仓端到端(连续加仓 3 次恒为 1 张)全部反向验证;**教训:"六处写同一条规则、且六处都只覆盖同一半情形"是按分支收口的典型症状,按返回值/不变式收口才能一次覆盖全部——这就是"别哪漏补哪"的具体含义**);**v1.16.15 该 bug 类第 11 次实例,已提交待部署 md5 e37b0c83 提交 75854df**(线上 SOXLUSDT 两档策略挂 4 张单/4 条 armed 记录且 dynamicOwner=4 verified=true;根因=drawdownRuleFingerprint 第 0 段是开仓均价,让 fingerprint 同时当"档位身份证"和"当时的开仓价",而开仓均价会被修正(place-at-open 用计划价 149.09 / 运行时用同步均价 149.07246479,加仓与部分平仓同样会漂)→ 身份分叉 → storedTrailingOrderIDForRule 查不到自己的单判"缺单"、armedFingerprints 判"没武装过"、nativeTrailingArmTime 300s 冷却一起失效 → 同一档挂第二张;**仓位身份那一层早就 entry→cTime 改过了,规则身份这一层漏了**;修法=新增 trader/drawdown_rule_identity.go,drawdownRuleIdentity 抹平 entry/qty 两段但**保持字段数不变**(auto_trader_risk.go:940/:1281 按下标解析 parts[2] 取 MinProfitPct),**比较与做 key 用身份、写库仍写带 entry 的原串**(线上记录不迁移即可认领,回滚安全);身份化 6 处 = armedFingerprints 集合/storedTrailingOrderIDForRule/claimedTrailingOrderIDsForPosition 的排除自己/isManagedDrawdownRecord/supersedeOlderArmedRecords(**分叉旧记录自动退役,老仓位自愈**)/reArmFailKey(否则熔断计数换桶永远攒不到 3 次);nativeTrailingArmTime 改 key 为 symbol|side|身份(**身份不含 entry 后不同币种/长短方向不能再靠开仓价碰巧不同来区分**)并在 resetPerPositionStateOnOpen 显式清理(key 不再自然过期,否则平仓后 300s 内重开挂不上单);**顺带修同一片状态的两个静默失效**:①drawdownState 一格两用——refreshDrawdownExecutionFingerprint 每轮往里写仓位 cTime,把"已执行"的规则串走 legacy 分支覆盖掉 → :469 重复平仓门禁永不命中,仓位身份拆到 drawdownPosIdentity;②恢复流程把 armed 的 native/managed 记录也写进 drawdownState(唯一读者就是那个"已平过"门禁)→ **重启后交易所挂单万一失效,managed 兜底会因"以为平过了"拒绝动手,直接违反陪跑双保险**,改为只有 status=executed 才进门禁;②以前被①的 cTime 覆盖掩盖着,**修一个 bug 让另一个从"被掩盖"变成"会发作",是拆共用状态时必须一起检查的**;测试 trader/drawdown_rule_identity_test.go 6 例全反向验证;**教训:身份里绝不能放会变的量;判断标准=这个字段会不会在对象生命周期内被修正,会就不能进身份**) /**v1.16.16 v1.16.15 的另一半,提交 `80845a2`,已部署**(只抹平 fingerprint 第 0 段并没有真的合并线上分叉——只读快照实测身份仍是 `*|*|3.8029|2.2818` vs `*|*|3.8025|2.2815`;因为**开仓均价在 fingerprint 里出现两次**:既是可归一的第 0 段,又是 ATR→百分比换算的**除数**,被烘进 MinProfitPct/MaxDrawdownPct 的数字里无法归一(3.8029/3.8025 = 1.000105 = 149.09/149.07246479);ATR 本身是按仓位冻结的(frozenATRCache),除数却用调用方当前的 entry = **半冻结**;修法=新增 frozenATRAndEntryForPosition 取回「ATR 是对着哪个 entry 冻结的」,12 处换算点(resolveDrawdownRulesATR/getActiveBreakEvenRulesATR/resolveATRProtection 的 SL/TP/ladder/BE/dd)统一改用 anchorEntry,取不到退回调用方 entry;结构止损的边界几何**故意不动**(那是价格几何不是 ATR 倍数换算);测试 trader/atr_anchor_entry_test.go 3 例反向验证;**教训:身份归一只能处理「独立字段」,被烘进数值里的量必须回到产生它的那一步去锚定 —— 归一是止血,锚定才是治因**) /**v1.16.17 该 bug 类第 12/13 次实例,提交 `87490ba`,待部署**(①trailing 归属判定是布尔量:classifyProtectionOrder 的 `else if nativeTrailingArmed` 让「任意一档 armed」就把交易所上**所有** trailing 单盖章成预期的 → 多余单永远进不了撤单路径;线上 BN/ETHUSDT 记录只认领 2 张交易所躺 3 张,多出的 0.067 半仓单止损价 1922.09 紧贴止损 1922.39,reconciler 每轮打 `dynamicOwner=3 unexpectedTP=0` 从不报警;SOXL 则是全平/部分档各两张,两张部分档同时触发平 60% 而非 30%(全平档那对靠 reduce-only 兜住,部分档没有);修法=新增 trader/native_trailing_ownership.go 的 nativeTrailingOwnership{Armed,Claimed},按认领的 order ID 集合判定(复用 claimedTrailingOrderIDsForPosition),并保留三条容忍边界(nil 视图/空集合/无 order ID 一律容忍在场单,撤错在场保护单后果更重),新增 StaleTrailingDuplicate 让「止损覆盖已满足就容忍多余止损」分支不再吞掉 trailing 重复单;profitOwner 仍只看 Armed,否则记录落盘窗口期会误判 missingProfit 触发无谓重挂 ②**「CL 分配表 1.1/0.33 vs 交易所 1.4/0.4」的答案**:drawdownTierAllocs 只存内存,重启后 auto_trader_risk.go:250 的 lazy-init 兜底重算时传的是**当前**持仓量,部分止盈砍过(1.4→1.1)再重启就按缩小后的量算比例(30%=0.33),而交易所上的单是开仓时按 1.4 挂的(0.42);arm 路径的数量匹配早已按 EntryQuantity 锚定 → 口径不一致 → 匹配不上 → 重挂 → 又一个重复单入口;修法=新增 entryQuantityAnchor,initDrawdownTiersForPosition 一律向 DB 取原始开仓量,取不到退回调用方量;测试 5+2 例全反向验证;**教训:比例的分母在仓位生命周期内必须固定(与 v1.16.16 同一条原则的另一半);所有权判定不能用布尔量只能用集合——布尔量回答不了「这张具体的单是谁的」,而多档并存系统里这正是唯一需要回答的问题**) /**v1.16.18 该 bug 类第 14 次实例,待部署**(OKX `tag` 字段上限 16 而 okxTag 恰好 16 字符 → okxReasonTag 拼 "<tag>_<reason>" 再截断**永远截回裸 broker tag**,所有 reason 塌缩成同一常量;这不是写错一行而是**字段容量根本装不下**,被一个「看起来在编码」的 helper 藏住;由此长出两个真缺陷:①cancelAlgoOrdersByTag 按 `tag == okxReasonTag(reason)` 过滤实际等价于 `tag == okxTag`,**命中该 symbol 上所有 bot conditional 单** —— 一次「只撤 ladder_sl」会连带撤掉其余止损档**和全部止盈档**(conditional 两者共用),而该函数自己的契约写着「持仓活跃时不得广撤保护单」,契约被静默违反;所幸目前**无生产调用方**(只有测试),属未引爆地雷 ②protectionReasonFromTag 永远匹配不上自家单,平仓归因静默降级为未归因;修法(全局非补洞)=删掉 okxReasonTag 并留长注释说明为何不能加回来,6 处挂单点直接传 okxTag 让 16 字符上限暴露在调用点,reason 只走 coded client id(clOrdId/algoClOrdId 32 字符,reason_codec.go 已有且线上已验证:ZEC 平仓 `via coded client-id=...STe0316ff998f9` 精确归因 structural_sl),撤单函数改名 cancelAlgoOrdersByReason 按**解码出的 mechanism** 精确匹配,导出 store.NormalizeMechanism 复用动态变体折叠(managed_drawdown_<stage>→managed_drawdown),**失败方向必须是「少撤」**(client id 解不出的一律跳过、reason 没注册 code 的整体拒绝,绝不退化成撤全部),补上 trailing 挂单点缺失的 algoClOrdId(此前其 reason 完全不可恢复),同步路径改为「先解码 client id,tag 启发式只作 legacy/外部单兜底」,DB 镜像 recordProtectionOrder 的 ClientOrderID 原先写塌缩后的 tag(所有保护单同一个值,列完全无用)改为落交易所真回的 algoClOrdId、取不到就留空**不用合成 nonce 回填**(伪造 id 匹配不上任何东西却显得权威);边界=老单无可解码 client id 时该函数退化为 no-op,刻意且不留缺口(真正清理走 cancelUnexpectedProtectionOrdersByID 按明确 algoId + 非活跃 symbol 广扫,均不依赖 coded id);**Binance 无此缺陷**(前缀 x-KzrpZaP9 只占 32 中的 10 余 21,且无 cancel-by-tag 路径,trailing 本就走 clientIDForReason,这也解释了为何只有 OKX 实现 okxTaggedProtectionCanceller);测试 trader/okx/cancel_by_reason_test.go 4 例含反向验证旧语义会命中 4 张;**教训:helper 的名字承诺了字段容量给不了的东西就会长出静默 bug,编码类 helper 必须让容量上限在调用点可见;凡「按标签批量撤单」都要先问「这个标签真能区分实例吗」—— 与 v1.16.17 缺陷 A 同病:用一个分辨不出实例的东西做实例级决策**) /**v1.16.19 该 bug 类第 15 次实例,提交 `17f8950`,待部署**(v1.16.18 部署后核对线上首个 OKX 开仓时发现:`cmd/orderlist` 的 `cid=` 列七张单全是 `4c363c81edc5BCDE`(16 字符裸 tag),而挂单日志里是 `4c363c81edc5BCDELS07f4fdfe415a`;同一时刻 BN 每张都不同(`x-KzrpZaP9LS…/LT…/NT…`)→ **同一字段两个交易所语义不一致必有一方错**;根因=`GetOpenOrders` 四处 `ClientOrderID: order.Tag`,且三个 algo 查询的结构体**根本没声明 `algoClOrdId`**,交易所回了也读不到;两个静默后果 ①**非空但零信息**:`enrichProtectionOrdersWithPlan` 只在 `ClientOrderID == ""` 时才跑基于价格的 plan 兜底推断,一个没用的常量把一条**算得出正确答案**的路堵死 ②trailing 分支 role 取自 `protectionReasonFromTag(Tag)`,tag 被 okxTag 占满后永远解不出 → trailing 单 role 恒为空;修法=三处 algo 查询补 `algoClOrdId` 并作为 client id 上报,role 走新增 `reasonFromAlgoIDs(algoClOrdID, tag)`(优先 coded client id,tag 仅 pre-codec 老单兜底),**legacy 老单上报空值而不是回填 tag** —— 空 = "不知道"可推断,裸 tag = "知道了但没用"堵路;与 Binance 适配器上报原始 coded client id 对齐;测试 `trader/okx/open_orders_client_id_test.go` 2 例 + `trailing_client_id_test.go` 2 例(挂单侧,提交 `89d0ae5`,test-only);**教训:字段名说 id,下游若拿它做明文子串匹配(`Contains(cid,"break_even")`),那些匹配器对 coded id 和裸 tag 一样匹配不到 —— 它们本就只是启发式,精确匹配走价格,别为迁就启发式往 id 字段塞明文;"非空但无信息"比空更坏,它会挤掉能算出真答案的兜底**) /**v1.16.20 该 bug 类第 16 次实例,提交 `815feae`,待部署**(巡检发现 `unexpectedTP` 由 0 变 2,该撤单动作本身是对的,但追"撤的是哪两张"发现撤掉 tier2+tier4 而日志声称已执行的是 tier1+tier2,**集合对不上**才挖到根因;根因=`reconcilePositionProtections` 整循环只读一次 `GetPositions()`(缓存)而 `reconcileProtectionForPosition` 内部**新鲜**读 `GetOpenOrders` → 夹在中间成交的档"挂单已消失+持仓量仍是成交前"→ **同一交易所两次读取新鲜度不同却当成一致快照**,只看数量时"档成交了"与"交易所丢了我的单"无法区分,而 anchor 只看数量;线上 CLUSDT SHORT(2.8@83.68,四档 20/18/15/12%):00:33:00 tier1 成交 0.6@82.71 → 00:33:08 anchor 在 `entryQuantity<=currentQuantity` 提前返回、四档全留 → missingTP=true → **tier1 被重挂在已穿越的 82.7193** → 00:34:03 重复单成交**又平 0.6** → 00:34:28 closedQty 变 1.2(tier1 量算两遍)贪心分配把富余记到 tier2 判其已执行,**而 tier2 的单全程活在交易所且从未触发**(需+1.77%,仓位只到+1.18%)→ 被当 stale duplicate 撤掉;**关键顺序证据**=`📉 Partial close 2.8→2.2` 落在 `🧾 plan materialized` 与 `Take profit price set` **之间**,是真并发竞态不是单纯滞后,故"下单前重读"只能补一个调用点;修法两条规则,都只用调用方已有的新鲜快照、不新增适配器接口 ①**外档还活着⇒内档已成交**(阶梯 TP 必按价格从入场向外依次成交,**这是唯一不要求两快照一致的推断,所以真正解决竞态而非缩小窗口**)②**单还活着⇒一定没打过**且该档不得消耗 closedQty(让活档分走一份正是内档多平记到外档的机制),无人认领的富余 closedQty **故意悬空不分配**;刻意假阴性=被外部手工撤掉的中间档会被读成已成交而不补挂,代价一个止盈目标,反面是在已穿越价重挂→平掉本该跑的仓位,`StopLossOrders` 全程不动**被让掉的永远不是保护**;原提前返回改 `noReduction` 标志(仓位"没缩小"恰恰就是滞后数量会宣称的东西);测试 `protection_ladder_anchor_live_test.go` 5 例复刻线上时刻全部反向验证(老逻辑报错即线上原样 `got [82.7193 82.1954 81.4967 80.536]`),原有 3 例传 nil live 价钉住数量算术未变;**教训:①同一数据源的两次读取新鲜度不同就是两个数据源,交叉判定前先问"这两份快照能不能不一致" ②修法要挑不依赖快照一致性的那条推断(价格顺序),否则只是把窗口改小 ③巡检里"计数由 0 变非 0"即使那个动作本身是对的也必须追到底 —— 本次正确的撤单动作是错误判定的下游**) /**v1.16.21 该 bug 类第 17 次实例,提交 `81cf187`+`5cef90d`,已部署**(用户从面板发现 DD1/DD2 **同时**红灯"缺失·待补挂",而同一时刻 reconciler 每轮报 `state=protected verified=true missingSL/TP=false staleTrail=0 manualForeign=0 dynamicOwner=2 claimedTrail=2` —— **面板与 reconciler 对同一份事实给出相反结论**;根因=`getDrawdownExecutionMode` 有 6 个 `native_*` 取值,其中 `native_trailing_tiers`/`native_partial_trailing_tiers` 是后来为"面板区分多档"新增的,而面板 runtime 的归属判断是字面量白名单 `mode=="native_partial_trailing" || mode=="native_trailing_full"` → **只要仓位同时武装 full 档(dd1)+ partial 档(partial_profit_lock),也就是现在线上的标准双档形态,mode 就是 `native_trailing_tiers`,两个字面量都不等**,整段按 algoId 找回交易所挂单的 ID 匹配被跳过,`matchedLive` 恒 false,`computeExchangeLight` 走 `if !matchedLive { return "red" }`;`is_armed` 同样被带偏显示"未布单";线上实测 CLUSDT/SKHYUSDT 双档 mode 均为 `native_trailing_tiers`;修法**不是把新字面量补进白名单**(那是哪漏补哪,下次加 mode 还会漏)—— mode 串同时编码**归属**(native/managed/本地兜底)与**形态**(单档/多档/full/partial),消费方只需要归属,故收敛成唯一谓词 `drawdownExecutionModeIsNative`/`...IsManaged`(按前缀判断、与生产方同文件相邻),任何 `native_*` 新值天然涵盖;同族并发修掉三处"手写并集各漏一个":①`managed_drawdown_armed` **根本没有 mode 映射**→掉到尾部按交易所能力返回 `native_trailing_pending`,纯 managed 兜底仓位被判"native 待挂单"→面板找不到单就红 ②`protection_reconciler.go:98` 保留动态状态的白名单漏 `managed_drawdown_armed`→交易所校验通过那轮把 managed 武装记忆覆盖成 `exchange_protection_verified`,下轮重复武装;`:523` 反方向漏 `managed_partial_drawdown_armed`,两处本该同一集合→统一为 `isManagedDrawdownProtectionState`/`isDynamicDrawdownArmState` ③面板 `nativeTrailingArmed` 漏 `*_arming` 两态,而 `nativeTrailingOwnership.Armed` 语义本就是"armed 或 arming"→武装窗口内面板把在场单归成"非我认领",与 reconciler 同时刻判断相反;另四处手写 native 状态并集统一走已有 `isNativeTrailingProtectionState`;**观测盲区(第二提交)**:该 bug 存活期内**日志一片干净** —— `protection_runtime`/`scheduled_tiers` 每次 `/api/positions` 现算、从不落库,红灯只存在于 HTTP 响应里,只能靠人盯面板发现且无从回溯;补 Warn:任一档红即打 `symbol/side/档名/mode/protectionState/交易所在场 trailing 单数`,健康时恒 0 条,走 8s fresh window+singleflight 单仓位最快 8s 一条;测试 `dd_light_multitier_test.go` 走完整 `buildPositionProtectionRuntime` 路径(**只测 `computeExchangeLight` 没用 —— 那函数从来没坏**),反向验证换回旧白名单即以线上原症状失败(两档 `exchange_light=red`),并钉住"单真不在交易所上时仍必须报红"确保没把真红灯抹掉;**教训:v1.16.17 是"所有权判定不能用布尔量只能用集合",这次是它的上游 —— 状态机的消费方不能用字面量白名单,只能用与生产方同处一地的谓词;白名单失效是静默的,漏一个取值不报错,只让整段判断悄悄不执行;而"非空但零信息"的误报(健康单报成缺单)比不报更坏 —— 它训练人忽略红灯**) /**v1.16.22 保护档「排序/单位/归因/落库」四处一次梳理,提交 `de6a3a5`,待部署 md5 `7ef98667`**(用户从面板发现两件事:①claude 交易员的回撤档大致对但"像是按触发价排的" ②BN 交易员的排序明显错;**核心概念订正**:一个回撤档有**两个价**,含义完全不同 —— 激活价 `entry×(1±minProfit)` 只是**开始跟踪峰值**、到这里不成交任何东西;成交价 `峰值×(1∓callback)` 才是真正落袋的价位。面板这一列排的是"接下来先碰到哪一道",所以键必须是成交价:一个"涨到 3ATR 才启动、回吐 1.8ATR 才平"的档实际成交在 **1.2ATR**,应排在 1.1ATR 与 1.7ATR 两个 ladder **之间**,按激活价排会把它顶到 3ATR 槽位 —— 这就是 claude 那台"大致对但顺序怪"的成因;修法=新增 `trader/drawdown_execution_price.go` 作为**唯一定义**(峰值锚 = 方向性 `max(激活价, 已实现峰值)`:激活前用激活价当下限=这一档最早可能的成交价,激活后随真实峰值棘轮),tier JSON 增 `execution_price`,前端 `price/sortPrice/deltaPct/atrMult` 全改读它;**②BN 排序错的根因是单位随交易所变**:binance/bitget 的 API 收发 callbackRate 用**百分数**(1.8=1.8%),okx 用**比率**(0.018),而早先代码在算完 `callbackRate` 之后**就地乘 100**,于是 tier JSON 里 `callback_rate` 的单位随交易所而变,前端只能靠 `">1 就当百分数"` 猜 —— 这个猜法在 **dd% < 1 时静默错 100 倍**(dd=0.54% → 发 0.54 → 前端读成 54% 回撤 → 成交价直接算飞);当前线上值(1.84/1.23)恰好 >1 所以是**潜伏**而非已发作,但潜伏的 100 倍误差不能留;修法=比率**全程贯穿**,百分数只存在于 `callbackRatioToExchangeUnit`/`callbackExchangeUnitToRatio` 这一对收发边界函数里,模糊匹配用的交易所形态收进局部量 `matchCallback`,交易所回读值统一归一;**顺带确认 Binance 的 `GetOpenOrders` 从不上报 `CallbackRate`**(classic-list 与 algo-list 两条路径都留 0,只填 ActivationPrice 并硬编码 `activated`)→ `applyMatch` 的 `cbVal>0` 覆写在 Binance 永不触发、qty+callback 模糊匹配在 Binance 根本不可能命中(靠 ID-first 匹配救着);**③落库归因不准/不全的根因**:`enrichProtectionOrders` **无条件**用四类粗分类 `classifyProtectionOrderRole` 覆写 `ProtectionRole`,把适配器从 `algoClOrdId` 解出的**细粒度**归因(break_even/ladder_tp/ladder_sl/fallback_maxloss/structural_sl,见 `okx/trader_orders.go` 的 `reasonFromAlgoIDs`)整个冲掉 → 落库和面板都只剩"这是个止损",分不清保本/阶梯/兜底/结构位;修法=细粒度优先(空才回退推断)+ 新增 `ProtectionRoleCoarse` **并存**,**粗粒度消费者必须同步改**(`auto_trader_decision.go` 里 switch `"stop_loss"/"take_profit"` 喂 ladder/full/fallback 计数的那段,不改就会因为细粒度值不再等于 "stop_loss" 而**静默停止计数** —— 这是本次唯一的"修一处必须跟着改一处"的连带点);**④快照落库(protection_plan_snapshots.tiers_json)四处补全**:回撤档补 `triggerPrice`(激活价)+`executionPrice`+`executionPct`(早先**完全没有价格**,所以根本参与不了价格排序)、结构位止损两级(收盘确认边界 + 挂交易所的安全网)**早先一条都没落库**现在补上并参与排序、整表按成交价排序(多头降序/空头升序,无价行沉底)、`giveback` 备注从 `%.0f` 改 `%.2f`(早先把 1.4142% 印成 "giveback 1%");结构位两级抽到 `resolveStructuralSLLevels`(`trader/structural_sl_levels.go`)**面板与快照共用**,否则两处各算一遍必然漂移;**读路径同步**:`api/placed_protection.go` 透传两个新字段并对**读出来的行**再排一次(老快照是按机制分组落的、回撤档当时无价),且把 legacy `close_intents` 回放路径也改成同一个价格序 —— 否则同一个面板里新仓位按价格排、老仓位按机制排,看起来像 bug;前端 `PlanItem.kind` 增 `structural` 并给色(与实时面板 `text-blue-300` 对齐),否则结构位行渲染成无色;**⑤顺带修开仓竞态(本轮巡检实测两次:07-28 04:44 ETHUSDT、05:24 KAITOUSDT)**:成交是**本地先确认**的(maker entry 轮询到 filled),交易所持仓接口要再过 4~6s 才返回这条仓位;窗口内 liveness gate 查不到持仓就把**活仓**判成"已平",走 `cleanupInactiveProtectionState` —— 那是**破坏性**的:撤掉刚挂好的保护单(ETH 那次撤了 5 张 algo 单)+ 清冻结 ATR/结构位,3 秒后重挂、面板红一轮;**关键区分:窗口内查不到的含义是"还不知道",不是"已经平了"** → 新增 30s 宽限期 `protectionFillGraceWindow`,窗口内**只推迟**这一轮保护动作(下一轮 poll 会补,推迟是安全的)、**绝不清理**;仓位确认可见后立刻清标记,不拖延真实平仓后的孤儿单清理;测试:`drawdown_execution_price_test.go`(含 3ATR/1.8ATR→夹在 1.1/1.7 之间的排序断言 + callback 单位往返 + dd<1 的 100 倍陷阱)、`protection_role_attribution_test.go`(细粒度保留 + 幂等)、`protection_fill_grace_test.go`(4 例含 nil 安全)、`api/placed_protection_test.go`(快照读路径与 legacy 路径同序)、前端 `PositionProtectionPanel.test.tsx` 新增一例**反向验证过**(改回按 activation_price 排即以线上症状失败);**教训:一个概念有两个数值时,必须先问"消费方要的是哪一个" —— 这里面板要的是"先碰到哪一道"=成交价,而代码给的是激活价,两者在多档系统里差一个 ladder 的位置;单位歧义要收在收发边界的一对函数里,一旦让"值本身"带上随环境变化的单位,下游只能猜,而猜法总有一段区间静默错 100 倍;"非空但零信息"再一次(与 v1.16.19 同病):粗分类覆写细粒度归因,字段永远非空却回答不了"这是哪种止损"**)/**v1.16.23 前后端枚举各自维护 → 真实机制显示"未知系统"(2026-07-28,提交 `f3ee04e`,已部署):后端 `store/attribution.go` 21 个平仓机制常量落库完整(`structural_sl` 已 15 行真实数据),但前端把这份枚举重复维护三份(`protectionPlan.ts` 的 `classifyMechanism()`+`MECHANISM_META`、`CloseAttributionPanel.tsx` 的 `MECH_LABEL`、`PositionHistory.tsx` 标签梯子),后端新增机制时无任何编译/测试报错 → 用户面板三笔真实结构位止损显示成"未知系统 raw: structural_sl";真源头是 `classifyMechanism()` 落到 `unknown_close`,标签表只是第二层;修法=不只补用户报的那一个,全量 diff 枚举补齐 5 处(structural_sl/max_hold/breadth_breaker/trend_reversal_flip/legacy_unknown),且 `structural_sl` 必须匹配在 `full_sl` 之前(镜像 `store/attribution.go:100`,否则粗标签抢先吃掉);**关键是补契约测试而非补标签**:`web/src/components/trader/mechanismCoverage.test.ts` 钉住全部 21 个后端常量(每个有中文标签/每个 raw 不落 unknown_close/structural 不被遮蔽+反向断言未配置机制确实返回裸 key),下次后端加机制测试先红;验证 tsc rc=0、vitest 142/142、线上 chunk 已含 5 个新标签;**部署踩坑:后端是裸进程占宿主 8080,`docker compose up -d nofx-frontend` 会连带拉起 `nofx-trading` 撞端口并整批中止(前端一度 3000=000),必须加 `--no-deps`,且 compose 项目目录是 `/root/projects/nofxmax` 不是 `/opt/webstack/nofx`**;教训:同一份枚举在前后端各自维护=一类必然复发的 bug,修法只能是让"不一致"变成测试失败;**v1.16.24**:ZECUSDT 每 2.5min 撤3挂3循环治本 —— 缺陷1 数量等价性拿量化前意图(0.0144)比量化后事实(OKX ctVal=0.01/lotSz=1 → 1 张 → 0.01)差 30%,5% 固定阈值必然在窄档误判,修法两边都过 `FormatQuantity`(交易所分辨不出的差异就不是差异),窄档仓位普遍潜伏非 ZEC 专属;缺陷2 anchor 的“已执行”只是快照推断,档位离开 plan 后不再消耗 allowed price → 在场单被当 stale 撤掉 → 推断事后成真且正反馈震荡,修法 `AllowedExtraTakeProfitPrices` 容忍但不挂不要求;**诊断陷阱:🧭 日志打 `ownership.MissingProfit` 而重挂分支由 `detectMissingProtection` 驱动,重挂那轮日志反而显示 missingTP=false;`verifyProtectionOrders` 只比价不比量所以数量 churn 全程静默**;提交 `9682f65` 待部署(md5 6958a152,备份 nofx.bak_zecchurn_20260728_044300)；**v1.16.25(提交 `7efc6cf`,待部署)三处诊断/判定缺陷收口:①🧭 日志改打驱动重挂分支的原始判定值+分歧时标 `ownershipMasked`(ownership 内部按 `missingTP && !nativeTrailingArmed` 掩蔽、且硬传 breakEvenArmed=false,两视图不可互换);②黄灯把 `markPrice<=0` 误标 `phantom` → 新增 `unknown_mark`(判不了≠确认已死),`!matchedLive` 分支经证在真实调用链不可达故改名 `absent_order_unexpected_yellow`;③runner 迁移的 `(trigger,callback)` 两个独立 if 可拼出跨订单的假参数对再喂给安全判定,加 `trailingOrders[0]` 兜底 ⇒ 可能撤掉部分止盈档却以为撤 runner,静默丢保护 → 抽 `resolveRunnerMigrationTarget` 候选>1/参数不全/匹配不上一律拒绝不猜(该路径前端 0 消费、日志 0 次,属潜伏);测试 11 例 Go+1 例前端,均带反向对照;**待部署二进制 md5 1b5a608a 同时含 v1.16.24+v1.16.25；**v1.16.26(提交 `b292774`)ZEC ping-pong 最后一张的根因:`anchorLadderTakeProfitToEntry` Rule 2 对在场档位保留**开仓量口径**的原始比例,而 2026-06-22 加入的可执行性门禁在 missing/unexpected 判定**之前**用 `当前量×比例` 过交易所最小量 ⇒ 在场时 0.08×12%=0.96 张<lotSz 被滤掉→在场单判 stale 撤掉,不在场时 reduction 路径算出 15%=1.2 张→判 missing 挂回来,**在场就不可挂、不在场就可挂**;两个改动各自都对、组合才出错(缺陷不在任何单个函数里,只在时序关系里);修法 Rule 2 也换算到当前量口径且仅在真减仓时换算;测试反向对照复现生产一字不差的 absent=15%/live=12%;**v1.16.25 已部署(13:38 pid 2479237 md5 1b5a608a),🧭 新日志生产首轮即打出 ownershipMasked 证明修复 1 有效;v1.16.24 只修好一半(TP 5→3 张、撤单 3→1 张,churn 未停)；**v1.16.26 已部署(14:25 pid 2526378 md5 e96205fd)且验收通过:6 周期内 materialized=0/cancels=0(修复前各每 2.5min 一次),🧭 稳定 protected/verified/missingTP=false/unexpectedTP=0;期间 467.23 档真实成交持仓 0.08→0.07,新量下 0.012/0.07=17.1% 过门禁而旧 12%→0.84 张仍会被撤,**证明该 bug 与具体持仓量无关、是比例口径错配的必然结果**;订正:验收标准不是"单数=7"而是"plan 档数==在场 TP 数且两侧都不动"** /**v1.16.27(提交 `f739c45`+`00ddfcc`,待部署)v1.16.26 部署后"再审视一轮"自查出来的(部署前快照 145 周期 cancels=3/🔴=6,而 v1.16.26 核对时两者为 0):**缺陷1 用"现在的 pnl"复判"下单那刻的决定"** —— `⚡ activation passed → immediate` 分支故意挂 activePx=0 单(锚点当刻固定在盈利处、只朝有利方向棘轮),而 `trailingOrderIsDangerousNoActivation` 拿之后某刻的 pnl 复判同一张单,同阈值/不同时刻/无滞回带 ⇒ CLUSDT short floor=3.1309% 挂单时 3.29%、漂到 3.12% 就撤、再挂再撤(5 次 🔴);该函数注释原断言"这绝不会是我们挂的"**被生产证伪而整个撤销逻辑建在它上面**;代价是实的:撤掉已生效的跟踪单会把交易所侧跟踪峰值重置到更差价格+裸奔窗口;**缺陷2 熔断承诺被 4/5 个调用点绕过** —— close=100% 档 14:44:31 跳闸明写"stop re-placing",但 14:46/14:49/15:04/15:10 仍在挂,因为 `applyNativeTrailingDrawdown` 5 个调用点只有 arm loop 那 1 个查 `reArmBreakerTripped`;**两者是同一条链**:熔断会跳不是交易所挂不上,是缺陷1 先把单撤了→判缺单→3 轮不生效→跳闸;修法=判别改问"下单那刻在不在门上",证据取持久化记录(activationPrice==0&&armed),**用持久化而非内存注册表因为重启后内存表必空、自己的单会被当遗留单全撤(该缺陷最坏形态)**;熔断门控下沉进 `applyNativeTrailingDrawdown` 成不变量,一次覆盖 5 个调用点;**第一版只修一半且是自查出来的**:`f739c45` 只修撤单侧,同样错误还在覆盖侧(`exchangeSideCoversDrawdownTierWithOrders`),抖动会把自己的单判 NOT effective→3 轮**误跳熔断**,而配合缺陷2 的门控修复会更糟(跳闸后本仓位余生不再维护该档),`00ddfcc` 改为同源判别;教训:**凡"当时判断→挂上→之后复判"的结构都要问复判用的输入还是当时那个吗**(与 v1.16.26 比例分母错配同属一个错误家族,一个是时间点错配一个是分母错配)、**承诺只写在一个调用点上就不是承诺**(机械检查法:grep 全部调用点数有几个查了门禁)、**注释里的断言也会过期且比代码更危险**;测试 `dangerous_trailing_intent_test.go` 6 例含 A3 pnl 来回抖动结论恒定 + A2/B2 防过度修正负对照,反向对照 A3 在 pnl=3.13% 精确复现生产翻转;**缺陷3(复审时在生产实时抓到,提交 `f5f6115`)幻影判定无容差带** —— 16:47 CLUSDT **30% 档**(另一条链)activePx=80.19/mark=80.15 只越过 0.05%,交易所仍报 pending_activation 即它没触发,但严格不等号判它幻影 → 3 轮误跳熔断;根因同属一类:**拿 A 系统的量判 B 系统的状态**(我们的 mark vs OKX 自己的 last/index 触发价源,边界必然差几个基点);修法复用 `protectionPriceTolerancePct`(0.2%),不漏真幻影因为要用到该档保护得先回吐 callback 量级(≈1.25%)、比容差大一个数量级;**三者是同一错误模式的三个实例而非三个独立 bug**:缺陷1 用错**时刻**(现在 pnl vs 下单那刻 pnl)、缺陷3 用错**价源**、缺陷2 是判断没错但**承诺没落在不变量上**;且互为前置 —— 修2 不修1/3 会把"停止重挂"变成永久关闭该档交易所侧维护,修1 不修3 会把撤单 churn 换成误跳闸,**只修其中一两个都会让情况更糟**(本轮最重要结论);二进制 md5 `4cd5f3f8458565f903e2d6abe10183e6`** / ****v1.17.4 平仓同步空窗的假警报：「无单可挂」被当成「挂单失败」（2026-07-30，提交 `0cb9d8c`）**：实盘 WLDUSDC 空头交易所 **03:00:03 平仓**、本地 `order_sync` 到 **03:00:22** 才落账，**19s 空窗**内保护层按本地仍持仓去挂 native trailing，adapter 两轮回 `no open SHORT position`，旧代码按挂单失败处理 → ① 给**已不存在的仓位** arm 了 managed-drawdown 兜底监控 ② 状态升级 `managed_drawdown_exchange_failed_armed` 点亮面板反色告警 ③ 最坏路径打出 `position may be UNPROTECTED`，三者全是假警报；**根因的对称性才是要点**：开仓侧早有 `protectionFillGraceWindow` 表达「还不知道」≠「已经平了」，**平仓侧一直没有对应闸门**；修法=binance adapter 加 `ErrPositionGone` sentinel（`execPositionAmt` 直接 symbol-scoped 调 `GetPositionRisk` **不走缓存**，`amt<=0` 是交易所本人的回答，判据可信），trader 侧 `trailingFailureIsPositionGone` **只认 sentinel 不做文案匹配**（交易所文案会变），命中则直接 `return false`，交回既有 inactive-symbol 清理收尾；**5 个 fallback 调用点只改 2 个**（full/partial 两个 error-driven 分支），另 3 个是结构性原因（空 orderID／跳闸）与此无关；**OKX 分支无此缺陷**（失败直接 return false，从不 arm 本地兜底）—— 这正是用户只在 Binance 看到它的原因；顺带发现**一处未动的既有不对称**：挂单失败时 Binance 落本地兜底、OKX 什么都不落，构成 3 个 OKX 交易员的保护覆盖差异，**改动会影响实盘保护行为故未擅动**；测试 `trailing_position_gone_test.go` 三例：假警报守卫（删掉分支必红）、**真实挂单失败仍须升级 exchange_failed 的对偶用例**（防止修假警报顺手把真警报一起吞掉）、sentinel 须穿透多层 `%w` 包装且**不误判同文案裸错误**** / ****v1.17.4b OKX/Bitget 挂单失败的观测漏洞 + 一处自我订正（2026-07-30，提交 `cce9794`，接 `0cb9d8c`）**：逐个交易所读代码时发现 native trailing 挂单失败**只有 Binance 分支用 Warnf，OKX/Bitget 全用 Infof** → 健康巡检按 `[WARN]` 取样，**3 个 OKX 实盘交易员的挂单失败在巡检里完全不可见**（我上一轮巡检就漏了，是逐个读代码才发现，教训：巡检不能只 grep WARN）；**必须订正上一轮的误判——OKX 不存在「保护裸奔」**：真正执行本地回撤平仓的是回撤监控主循环 `auto_trader_risk.go:357-410` 的 co-run 段，它**每轮无条件评估**、判据是档位分配 `allocs`+逐档 `exchangeSideCoversDrawdownTier`，**与 protectionState 无关**（注释 "CO-RUN, NEVER HAND OVER"），`applyExchangeFailedLocalMonitor` 对全平档只写状态不注册执行者、**从来不是执行者**，我把「失败时没调兜底函数」错读成「没有兜底」；生产实证 co-run 陪跑 **5777 次**，XAU/SPCX/ETH/SOL/BTC 全在 OKX 上都有；面板灯也无差异——`matchedLive=false` 直接红灯与交易所无关，OKX 失败照样亮红；**「Binance 立刻挂本地兜底、OKX 等跳闸(3 轮≈30s)」是 churn 风险 vs 崩溃窗口的既有取舍不是漏洞**（"native-partial-capable 不应每轮 fallback" 是明写的设计），故不动；修法=okx/bitget adapter 各加 `ErrPositionGone` sentinel（**提级 WARN 的前提是先能区分「无单可挂」**，否则平仓空窗把假警报批量塞进 WARN，等于把 0cb9d8c 修好的噪音换个交易所再犯），`trailingFailureIsPositionGone` 逐个认三个 sentinel（值同类型异，故 `errors.Is` 要逐个），新增 `logNativeTrailingPlacementFailure` 统一 7 个失败出口(position-gone→Infof 静默，其余→Warnf)；测试守「三 sentinel 穿透多层 %w 且不误判裸错误」「三者互不 Is」「记日志不得越权改保护状态」；**部署前整体巡检结论**：ERROR/panic 0，WARN 857 条全良性(API 503/524 已 fallback、reconciler 日志、订单回收)，`UNPROTECTED` 仅 2 条正是 0cb9d8c 修的那个 WLDUSDT(03:00:04/13，修后静默)，进程单次启动运行 9.5h 无重启，DB locked 2 条是 giveback_guard best-effort 记录撞锁(良性)**) |
| `binance-native-trailing.md` | BN 原生 trailing 修复（go-binance v2.8.9→2.8.10 参数键名 bug、algo 端点、close 缓存裸仓修复、遗留 issue 1/2） | 活跃 |
| `exit-timing-stop-research.md` | 退出侧研究(已逐K定论,未部署)：弱势/动量判据无增益(证伪)；快照仿真的2×ATR收紧止损被逐K回测证伪(claude上-45U最差)；**四trader逐K验证:无固定N×ATR能全正,系统现有swing结构close-confirm止损已是稳健最优,勿替换**；顺带发现Claude-R 15m止损偏紧可单独跟进 | 已定论 |
| `ai-decision-parse-robustness.md` | AI 决策解析形状容错：模型填写的字段形状是不可控输入,不是契约；2026-07-28 一个裸字符串 `alignment_notes` 丢掉整周期 3416(含 ZECUSDT open_long)→ `AIStringList` 容错,三个 AI 填写字段一起换,币池填的 `Sources` 故意不换；带反向对照；**结构性根因(c21e3c3):整数组原子解码——实测一个坏字段会让解码在该元素处整体中止,该元素清零且其后所有决策根本不被解码(含 close,漏平比漏开危险)→ 改逐元素解码,类型错误隔离/语法错误仍整批失败/一条都救不回仍报错不返空集,降级经 ParseFallbackReason 可见** | 活跃 |
| `production-deployment.md` | 生产部署全量记录（**最新:2026-08-02 AI 备用端点按 Priority 排序 + 6 端点全通上线 pid 641545 commit `ede8ea9`**,md5 `7215def1` ← `5acea85f`,回滚 `/root/.claude/jobs/5cbb3cf4/tmp/dbbak/nofx_prev_5acea85f`。**根因是密钥拓扑不是网关**:claude/openai 主密钥曾是同一失效 token,而 `switchToNextEndpoint` 对空 `api_key` 沿用当前密钥 + 每次调用前 `restoreToPrimary()` → **空密钥备用端点永远拿主密钥去打,主密钥一坏 3 个备用同时陪葬=没有备份**;首测 1/6 → 换密钥 4/6 → 填最后一个 key 后 **6/6**。**修的 bug**:`switchToNextEndpoint` 只按下标推进从未读 `Priority`(注释却声称按优先级),生产 claude 行 NovaI(prio=2) 在前、lt48(prio=1) 在后 → 实际先打低优先级;修法=`SetFallbackEndpoints` 入口一次稳定排序(配置期而非切换期,不变量一处;作用于副本以免污染 `GetFallbackEndpoints` 调用方),新增 4 测试并摘掉排序验证过会失败。**DB 只改一处**:claude `fallback_endpoints[0].api_key` 空→填值(openai 同位置库里已正确,未写)。**⚠️ 未解决**:① claude try#1 仍靠继承主密钥、自身 key 为空,主密钥失效则主+try#1 同时挂 ② 备用密钥库里**明文**而主密钥 AES-GCM(`UpdateWithFallbacks` 直接 `json.Marshal` 不走 `EncryptedString`)。`us.novaiapi.com` **不是坏是慢**(16.78s,60s 超时会误判失败;但 `auto_trader_breakout.go:199` 压到 20s,贴线,用户说超时没问题未改);已确认这些 OpenAI 兼容网关**支持 Anthropic 原生 `/messages`**。探针 `cmd/aiendpointprobe` 已进仓库(复用生产 provider 客户端+复刻继承与排序语义,`mode=ro` 只读,密钥仅掩码)。**上一次:2026-08-01 复盘 ATR 上下文 + 屏蔽低波动独立开关 pid 579360 commit `9a48070`**,md5 `69a26400` ← `37dfd393`,回滚 `/root/.claude/jobs/5cbb3cf4/tmp/nofx.rollback_7e27aff_20260801_1930`。**前端也重建了**(nofx-frontend 是 Docker 容器不是静态目录:`docker compose build` → `up -d --no-deps`,bundle hash 与本地一致,api_proxy=200)。**BE 档位是显示缺陷不是逻辑错**:`triggerPct` 是激活阈值、`triggerPrice` 是按 offset 停放的止损价,两个不同价位被并排渲染成 `BE1+1.3%@1856.69`;按 entry 1862.28 核对 BE1 offset 0.3%→1856.693、BE2 offset 1.0%→1843.657 全对。**潜在真 bug 仍在**:解析被跳过时会把原始 ATR 倍数当百分比打印。BE 配置何时改的查不到 —— `trader_protection_config_history` **0 行**。**ATR 必须落快照**:`DeleteFrozenATRRecord` 平仓即删,故 `protection_plan_snapshots` 加 `atr_value`/`atr_timeframe`(生产已加列,319 行保留,旧行=0);读取必须用**新增的 `frozenATRForPositionReadOnly`**,复用 get-or-freeze 变体会让上报路径**铸造交易状态**(未命中时实地取值并写库,可能把持仓绑到开仓数小时后的 ATR)。**⚠️ 我自己犯的错**:第一版把低波动 floor 放在 `evaluateMarketStateGate` 的 `if !regimeCfg.Enabled { return }` **之后**,而生产 `regime_filter.enabled` 在**4 个实盘策略里 3 个为 0**(仅 BN-chart+ 开)= 重犯 `entry_gate.min_atr14_pct` 挂在 `entry_structure.enabled` 下的老问题(可设置永不执行),正好违反"不依赖父开关"。已提到早返回**之前**。**8/8 测试全绿没抓到**,因为所有测试都用 `Enabled: true` → 补 `TestVolatilityFloorIndependentOfRegimeFilterSwitch`,并断言天花板**仍**留在早返回后。同形态另有两份:`entryPipeline.ts` 的 `active` 跟父开关(会把正在拦单的门显示成未生效)、`api/strategy.go` 校验包在 `if regime.Enabled` 里(恰好在"父开关关+floor 开+阈值 0"最需提示时静默)。字段挂 `RegimeFilterConfig` 只为**分组**不是依赖。**默认关闭是数据决定的**:519 笔按品种各自 ATR 中位数切半,低波动半 234 笔 63% 胜率 **+20.01**、高波动半 239 笔 52% **-132.69**,11 品种 8 个同向 —— **低波动是当前赚钱的那一半**,开启会砍掉盈利来源;只针对 ATR%~0.1 的手续费尾部(0.9-ATR 目标 0.106% vs 往返 0.12% = 占毛利 113%)。实测数字写进代码注释防后人顺手打开。零行为变化已确认(5 策略两键全 ABSENT、`getRegimeFilterConfig` 不套默认值、`GetDefaultStrategyConfig` 只兜底不回填)。**min_eff_pct 塌陷仍未修**:地板不能去(0.118% 时目标 0.106% < 成本 0.12%),但导致 TP1/2/3 全钳到同一价、53% 仓位+BE1+Drawdown 挤一处;315 快照 11 个重叠,0.118% 是第 **0.4** 百分位(P50=0.759%)。用户要求**按 0.1% 精度逐个回测**再决定**）（**最新:2026-08-01 RR 审计双 plan + 覆盖率维度上线 pid 532689 commit `7e27aff`**,md5 `37dfd393` ← `48edc644`,回滚 `/root/.claude/jobs/5cbb3cf4/tmp/nofx.rollback_48edc644_20260801_115556`。纯审计路径只打日志。原钩子在丢档**上游**,字段只能叫 planned_*,ZEC 那次 65% 只挂 48.3% 审计一行没提示 —— **掉的是最远档,nearest RR 一模一样**,所以补 `ProfitCoveragePct` 作为唯一能暴露梯度变窄的维度;**坑**:塌缩后 plan 是单个全仓 TakeProfitPrice、梯度为空,对梯度求和会把 100% 报成 0%、读起来像保护全丢(恰好相反),须回落单价字段判 100。`auditPlannedAndPlacedRiskReward` 对两个 plan 各审一次(纯函数由调用方跑两遍,第二遍 quiet=true 跑挂单路径即将用的同一过滤器),**放在 applyPostOpenProtection 而非 placeAndVerifyProtectionPlan 内以避开重试循环**(否则每次重试打一行)。placed_* 是忠实预测非保证(mark 是挂单前几微秒读的)。实测三形态:塌缩 cover 65%→100% 且 RR 0.53→1.41(向上塌缩收益直接可见)/ 部分变窄 declared 偏差 **+0.00** 却 cover 65%→38%(旧逻辑漏掉的正是这类)/ 健康无输出。部署后 4723 行 0 ERROR 0 WARN 0 撤单**）（**2026-08-01 保护档位向上取整 + 塌缩改最远档 pid 524730 commit `a7ff9e3`**,md5 `48edc644` ← 上一版 `ab68255b`,回滚二进制 `/root/.claude/jobs/5cbb3cf4/tmp/nofx.rollback_ab68255b_20260801_110517`。ZEC 12%/3.6-ATR 档算出 **0.99305275 张,差 0.007 张不到 MinSz 1** 被在取整前判死 → 配置 65% 只挂上 48.3%;历史 1690 笔中 **181 笔(10.7%)少挂至少一档**,平均覆盖率仅 34.4%。根因=**两套量化规则打架**:四条挂单路径"向上夹 MinSz 再 formatSize",校验器拿未夹取值比 MinSz,与 2026-07-28 挂撤循环同类。修法:`protectionSizeForQuantity` 单一量化入口 + 校验改判解析后张数 + `ladderWouldOversell` 守卫(部分跌破放行,全档跌破仍塌缩;**等号也拦**——`intendedPct<100` 的部分梯度若吃满全仓则丢弃,单档 100% 止损放行) + 塌缩最近档→**最远档**(选最近档等于把全仓封在第一个减仓位,ZEC 量级加权 RR 比不挂还差 **-0.035**;下行由 BE1 1.3/BE2 2.5/dd1 2.5+1.5/giveback 1.15 ATR 独立承担)。回放 1690 笔:181 笔覆盖率 **34.4%→67.9%**,最大 80% 无一超 100%;76 笔走塌缩,34 笔若无守卫真超发。**部署教训:`cp` 覆盖运行中二进制会 `Text file busy`,restart 后仍跑旧码且 md5 不变,必须 stop→cp→start 并用 md5 确认**;**核查切分不能用 awk 字符串比时间戳**(非时间戳开头的行里字母 > '0' 会误匹配),须锚 systemd `ActiveEnterTimestamp` 对应的启动横幅行号。部署后 3096 行 0 ERROR/0 WARN、77 条 verified=true 无一 false、0 撤单,207 条 `not re-placing` 证明防挂撤仍生效**）（**2026-07-31 HH/HL 结构闸门上线 pid 345250,**备份 `nofx.bak-20260731-030515` md5 abca7a86,配置备份 `/tmp/cfg_backup_20260731-030515/`,只对 claude-ct30+GPT-ct50 开启强制模式,Claude15 必须排除因 15m 效应反向;上线即刻 11:22:10/11:22:37 打出两条 `structural_alignment_missing`,第二条只此一项失败是最干净证据;同日修复 `bars=21` 窗口缺陷。回滚:停服→回二进制→恢复配置备份;若只关闸门可 `json_set` 置 `structural_alignment=false`,配置项独立**）（**v1.17.3 超额敞口收敛已上线 2026-07-29 pid 84853 commit `ba2a2e9`**(备份 `nofx.bak-20260729-175747` md5 921f4a0b,新 md5 17f3abfb):部署前实测危害 ETHUSDT 空头 **4 张活 trailing 其中 3 张全平**、协调器却一直 `staleTrail=0`,四张同属 `dd1` 只是 ATR 倍数被重解析过(1 ATR=0.85564% → 1.20/**0.90**/1.80(30%)/1.50ATR(当前设计))→ **谁先触发谁平整仓,ETH 会在回撤 0.7701% 被整仓平掉而设计 1.2835%,提前 40%**;上线第一个周期 01:58:15 打出 `Over-claimed trailing exposure ... 4 live claimed trailing orders for 2 configured tiers` ×2,21 秒后 `✓ Canceled algo order by id` 两张 —— **端到端闭环**(退认领→交回既有 stale 清理→交易所真撤);ETH 4/4→2/2、账本 12 armed→10 armed+2 superseded、5 仓位全部 `armed=2 oids=2 ratios=[30,100]`、ERROR 0 panic 0、health 200@2.1ms;**排查陷阱:日志按日期滚动,先 `ls -t data/nofx_*.log | head -1`**(我差点误判"修复没生效",旧文件末尾是上个进程的 `System shut down`);**订正我自己的误读**:`dynamicOwner=3 claimedTrail=2` 不是"有单没人认领" —— 完整字段是 `dynamicOwner=3(trail=2 be=1)` 含 1 张保本止损,**两个计数器分母不同不能相减**,WLD 是健康的(且它同时被 OKX/Binance 两个交易员持有,各 2 条 armed 属正常）** / **每次上线的 pid/配置备份路径/回测依据/回滚点；含 BN 回撤档展示口径修正+交易所 StopPrice 语义不对称、切换交易员去阻塞、进程移交 systemd 与 ExecStartPre 重启风暴教训、BE 保本止损档位收敛、v1.17.2 多档 DD 档位标识真正生效（pid 66117/a1114fc，冲突 2→0、reclaim churn 980→0）、positions/history 性能修复、统一止损带、min-SL-distance 门禁等） / **最新:2026-08-01 Claude-R 15m 闸门参数化+RR 审计上线 pid 494713,md5 `ab68255b`,备份 `nofx.bak-20260801-044713`,commits 9d1c269/fac0f1e/d3add9f/5035861;顺序不可换=先二进制后配置(旧二进制下 `adx_period=10` 会静默回落 ADX(14)+阈值30=实测最差格 -0.120);Claude-R 配置 counter_trend(96)/chop_reject/adx_weak(阈值30 **period 10**) 全 enforce + structural `lb=4 n=3 pct=0` **audit_only**;13:16 CST 周期四项变更全部实测到;RR 审计首个发现 ZEC declared=2.32 vs placed_nearest=0.35 算术正确,但 **manual 模式下该差值衡量的是「AI 论断 vs 操作者自己的阶梯」而非 AI 夸大**,且仅 ~65% 仓位走阶梯其余走 trailing,不可当 AI 准确度指标** | 活跃 |
| `volatility-floor-collapse-backtest.md` | **波动率 floor 与档位塌陷回测(2026-08-01,结论=两项都不要做)**。⚠️ **门禁读 1h ATR 不是 15m**:生产路径 `GetWithTimeframesExchange` 从不给 `IntradaySeries`/`LongerTermContext` 赋值(恒 nil)→ `computeATR14Pct` 必落 `TimeframeData` 回退分支 → **1h 优先**,四策略 `selected_timeframes` 全含 1h(含 primary=15m 的 Claude15-chart)。验证:`peak_pnl_pct/peak_atr_mult` 反算出的是**保护档位**冻结的 ATR,与 1h 吻合(中位 1.0011,98.8% 在 ±25%),与 15m 是 1.97(差 √4)—— **门禁与保护档位读不同 ATR**。**floor 扫描 1663 笔基线 −93.52**:0.3%→**−5.73**、0.5%→−0.33、0.7%→−37.31、0.9%→−55.76、1.1%→−60.27,**1.1% 以下每档都是负的**(被拦掉的单胜率更高:63.2% vs 55.2%);1.2% 才转正 +10.15 但已砍一半仓位、且不给它"躲过最惨一单"的功劳就翻成 −11.01;1.3%+ 稳健但砍 55–86% = 关机不是过滤。**手续费假设被否**:低 ATR 桶手续费占比确实最高(0–0.2% 是 0.40 vs 1.0–1.5% 只 0.05)但那两桶**净盈利**;亏损集中在 **1.0–1.5% 桶 428 笔 −150.21**,那里手续费不是问题。**1.1–1.3% 中间带**排除后 +135.86 且过了全部稳健检查(13/15 品种为负、剔任一品种仍 ≥+103、5 交易员同向、10/13 周跑输同周),**仍不采用**:搜了几十个组合挑最好的=过拟合,96% 亏损来自 3 周(55 笔),claude 占 70%,**说不出机制**。**塌陷是问错了问题**:各策略地板不同(claude/BN=**0.5**,Claude-R/GPT=**0.3**,且 `atr_protection` 在配置**顶层**不在 `protection` 下),重叠 84/1370=6.1%,而 **84 起全是 BE+TP 撞价、TP-TP 撞价 0 起** → 不是收益问题(出场价没变),而是 v1.17.6「一张单两个主人」同形态,但后果已被挡住:①`isBreakEvenTaggedOrder` 要求 `looksLikeStopLoss` 而后者显式排除 TAKE_PROFIT/TP → 身份撞车不可能;②各 venue `NativeReduceOnly:true`,真实成交验证 83 笔中 `SUM(close_ratio_pct)>105%` 的 **0 笔**;③受影响 84 笔净 **+4.17**(胜率 59.5%)vs 未受影响 1286 笔 **−86.66**(58.2%),`break_even_stop` 36.9% vs 26.2% ——同价时 BE 确实易赢但那是**在地板价锁小赢**,方向与"塌陷有害"相反。上界 +6.87(已假设 MFE 完美出场)对比窗口 −88.59 = 7.8%,**天花板太低故未做真回放**。复用:`trader/backtest` 有完整回放引擎+bar cache,既有 gob 覆盖 05-26~07-25(窗口内 1327 笔) |
| `equity-circuit-breaker-backtest.md` | **账户权益熔断回测(2026-07-29,结论=不要上线到 claude)**。回撤触发信号在三个交易员上**未通过安慰剂检验**(claude 2/98、GPT 2/98、Claude-R 10/81,98 次检验 p<0.05 偶然期望约 5 → 与噪声同阶);做功的是 `arm%` 止盈效应而非回撤阈值(arm=12 时 drop 1% 与 20% 同为 -52)。1.5-4% 先天不可能:权益回撤%≈杠杆×价格波动%,claude 有效杠杆 3.7x → 1.5% 权益回撤仅约 0.4% 价格波动,在 tick 噪声内。**顺带修掉 3 个既有引擎根因**(commit `b525a40`):①`realizedPnL()` 漏乘 `exitFrac`,部分平仓在飞行中按全量记账(30% 减仓算 3.33 倍),而组合权益曲线每 bar 都读它、熔断正在该曲线触发 → 此前所有熔断结论都基于被放大的权益序列;既有测试全在完全平仓后断言故无法发现。②loader 读 `quantity`(实时剩余量,claude 79/557 为 0)而非 `entry_quantity` → 尺寸错误且 79 仓被静默丢弃(仅载入 478)。③`.gitignore` 的 `backtest` 无前导斜杠 → 静默排除整个 `trader/backtest/` 源码目录,新增文件提交即丢失。**采用权益冻结模型**(`L3Cycle*`):开火=全仓按 mark 平掉,结晶而非毁灭价值、曲线连续,冻结点后交易员**真实**开的仓构成新周期;只截断真实仓位、从不创造敞口 → 各参数下开仓样本一致才可比。弃用的重开仓模型因虚构敞口复利放大曾造成 +98 次开火非单调。**两个必须记住的自纠**:首版安慰剂全跑 arm=0 得出错误的"54/90 战胜硬币"(arm=0 时 100% 武装且开火均匀,arm=12 只在盈利后约 3% 时间武装 → 次数同而时机分布完全不同),按 arm 配对后才是真结果;单调性检查器曾把安慰剂行(`dd=999%` 哨兵、仅种子不同)纳入分组,把种子离散度误报为 +76 次违规。新增 `-cyclemeasure` 探针(只测量不动仓位)三交易员真实数据全部 `monotonicity OK` → 触发测量正确,残留非单调来自"开火重置回撤峰值"的内在反馈。所有结论均在 `FaithfulExits`+手续费开启下得出(claude 63% 平仓来自本引擎未实现的机制;手续费必开否则优化器疯狂开火)。**BN 已覆盖**(经 `proxyhook` 代理,150/151 仓)。**第二轮(0.1% 精扫+时序训练/测试切分,commit `02fa090`)决定性证据:参数不可提前选择**——粗网格 arm 只有 {0,1,2,3,5,8,12} 故**从未跑过争议的 arm=4%**(教训:漏掉争议点的网格无法下结论);补 0.1% 步进 1260 格后,claude 正向选 dd=3.2/arm=2.5 训练 +97.53→样本外 **-22.64**,反向选 dd=1.9/arm=0 训练 +65.04→样本外 +13.79,两方向**互为对方的事后最优**;争议配置 dd=2/arm=4 正向 +96.04→-16.86、反向 -16.86→+96.04,其邻域 55 格正向 **0/55** 为正、反向 **55/55** 为正;四交易员留存率 claude -23%/21%、GPT 160%/-99%、CR 6%/249%、BN 0%/-217%,**符号随切分方向翻转**=测到行情差异而非可学习参数。**正确做法是直接减仓**(对照4:无参数、无换手,claude 50% 仓位训练+40.57/样本外+22.78,10% 仓位 +73.02/+41.01,**双侧同时改善且单调**)。⚠️ 用户直觉「降低敞口是正期望」**正确**,但收益来自少持仓、与回撤阈值无关;「优于基准」与「不如掷硬币」并不矛盾——配对安慰剂同样等 +arm% 武装/同样付费,唯一区别是出场时机,真实规则出场价恒为「峰值-dd%」而安慰剂随机出场其均值相当,故 dd 不携带未来信息;而在亏损账本上任意截断都倾向改善盈亏,故两句话同时为真。**回撤%按原始权益不除杠杆**(148→146.52=1%),已由测试钉死。**第三轮(改用真实权益曲线,commit `3826b51`)找到失效根因**:前两轮都在分析**回放重建**曲线,而系统存着真实记录 `trader_equity_snapshots`(claude 3281 条),新增 `-realcurve`/`-ddstat`。真实曲线 起 234.85→峰 299.61→现 146.52,22 个 episode,触及 2% 仅 **3 次(2 次自行恢复)**。**决定性: 峰值出现在 6/23 即 44 天里第 4 天,之后 90% 样本都在峰下且再未回去** → 「距峰回撤 m%」不是偶发保护而是**第 5 天拨下就再没抬起的开关**,这同时解释了阈值选择为何无意义、切分为何翻转符号。分桶 24h 前瞻证明方向相反: 0-0.5% **+4.00%**、1-2% +1.15%、**2-3% +2.61%**、3-5% +1.92%、5-8% -1.60%、**>8%(占 87%) -1.06%** → **争议的 2-4% 一带前瞻为正,开火即卖在反弹前**;只有 >5% 转负,且先前"回撤中前瞻更差"完全由 >8% 桶主导,实质是"90% 时间深水+全程负漂移"。⚠️ **自纠(方向上曾偏向支持熔断)**: `simulateFreeze` 初版只在原始曲线创历史新高时解冻,而暴跌不恢复的曲线永不创新高→一次冻结后永久不参与,建模的是「在高点附近永久停止交易」,曾报出 **+124.78 假收益**(每个阈值都 BETTER)。**教训: 权益曲线上做回撤熔断研究必须显式声明重入规则,否则测到的是市场择时。**已改显式重入并增印 `never` 列,该列(233/235/263)也全赢 hold(146.52)=停手本身就拿到大部分收益。**第四轮(改用正确目标函数,commit `373dc59`)**: 前三轮按 PnL 排名是错的,「稳定爬楼梯」是**路径形状**目标(翻倍再腰斩可在 PnL 上胜过稳步上行);改用 **Ulcer Index**(全样本回撤均方根,同时罚深度与**持续时间**)+**Martin 比率**排名,新增 `-stair` 扫 4320 格。maximin(按较差半段排名+要求 inMkt>=60%)最佳参数: **claude 涨+5.0%/回撤6.0%/重入3天**(全程 -37.68%→**+19.64%**,maxDD 53.69%→30.12%),GPT 2.0/3.5/1天、Claude-R 2.5/0.1/7天、BN 0.5/0.6/1天——**四者无共同值,不可跨交易员迁移**,且 claude 答案是 6% 不在用户提的 2-4% 内。⚠️ **但这是孤立尖峰**: 固定 arm=5/delay=216 变动回撤 1%→-1.569、2%→-1.430、3%→-1.433、4%→-1.177、5%→-1.289、**6%→+0.281(唯一为正)**;固定回撤=6% 变动延迟 6→-1.194、18→-1.129、72→-1.367、**216→+0.281(唯一为正)**、504→-1.537、1008→-1.827 —— **两轴上每个邻居都是负的**,4320 格中两半段同时为正仅 251(5.8%);且首版延迟上限 216 时最优是 1.5/0.1/216(样本外+8.27%),**仅把上限扩到 504/1008 最优就搬家到 5.0/1.2/504(样本外-10.66%)**=拟合噪声确证。⚠️ **必须与平滑度指标并列打印 TimeInMktPct**: 冻结区间完全水平且水平样本被计入'处于高位'→**空仓本身就机械改善 Ulcer**,不打印则优化器必然收敛到'停止交易'并伪装成调好的熔断(claude 首版最优 inMkt 仅 21.5%)。⚠️ **自纠**: Ulcer=0(完美楼梯)时 `>1e-9` 守卫得 Martin=0,**反而排在 boom-bust(0.31) 之后**=度量在自己最优点上是错的,已改 +Inf(真实曲线无零 Ulcer,扫描结果不受影响)。 |
| `market-structure-map.md` | 市场结构地图升级（进行中）：结构统一为区间+来源+行为+新鲜度+强度+角色+失效条件；核心原则=结构是提升信心的证据不是硬门禁,与现有 10+ 处硬校验冲突待 Phase3 改 / **时段开盘区间模块已落地(2026-07-30,纯证据未碰 gate)**：`market/session_range.go` 三时段锚点(ASIA 00:00/EU 07:00/US 13:30 UTC)+开盘 15m 区间+日ATR消耗度,只用已收盘 bar(未收盘 bar 不进区间),粗于 1h 的 timeframe 返回 nil;**关键校准结论=原始 OR/日ATR 不能用固定档位分级**：Doug 偏好 70-80% 来自单时段股票市场,crypto 24h 连续交易下 15m 窗口只占全天 1/96,原始比值必然贴近 sqrt(15/1440)=10.2%(实测 14天×12币 p50=9.7%/p99=52%),用 20/40/70% 分档 → **74% 挤在 compressed、0.3% 落 exhausted,与 `isRangeMiddleRegime` 里那条死掉的 `"range"` 分支同一类无区分度缺陷**;修法=除以随机游走期望 sqrt(window/1440) 得量纲无关的 `ExpansionRatio`(1.0=窗口时长应有的宽度,实测 p25=0.6/p50=1.0/p75=1.9/p90=2.9),档位 compressed<0.6/normal 0.6-1.3/expanded 1.3-2.5/exhausted>=2.5,复测四档均有量且 US 时段 exhausted 占其自身 20% 最高符合预期;原始 % 仍输出但仅供与文献对齐**不用于分级**;exhausted 档才附"追已耗尽的波动"提示(其余档附了会压制所有顺势单) / **2026-08-01 `session_range` 已提交入库 `9d1c269`(此前从未在 git 里,属未追踪文件)**:357 行,`CalculateSessionRange` 仅在 `barMin<=0||barMin>60` 时返回 nil,只用已收盘 K 线,渲染进 prompt 供 AI 参考,**永不拦截、永不参与仓位计算** | 进行中 |
| `entry-gate-research.md` | 入场门禁研究总账（2026-07-30，**全程不碰实盘门禁、代码零改动**）：一句话=**决策时点的预测型门禁已到顶不要再加，可动杠杆是几何与单位不是预测**。**AUC ~0.50 天花板两路独立确认**：决策时点组合分数 walk-forward OOS LONG 0.5056(8/16季,p=0.598)/SHORT 0.5030，十分位实际不利率仅 0.3304→0.3486；入场后察看期去掉同义反复后 0.455–0.512（`probation.py` 的 AUC 0.68→0.88 是**同义反复**：结果由路径定义，fav≥0.5R 机械 arm 保本、adv≥1.0R 机械止损）——与 `coin-direction-vendor-eval.md` 的 0.4999 一致。**七条候选全否**：局部特征选方向≈0；LONG/SHORT 不对称是 **beta 非 skill**（与大盘相关 −0.838，UP季 −11.29/DOWN季 +27.62 反号）；ADX/ATR 弃权格 bps 上 p=0.0035 完美单调但**ATR 单位下归零**（负期望×更大 ATR = 更大负 bps，杠杆假象）；K 门禁与 `MinATR14Pct=1.2`（隐含 K>=10.3）冗余且阶梯 TP 下减半；K×netRR 交互虽真但实盘 `MinRiskRewardRatio=3.0` 正落在 K 增量为 0 的死区(p=0.41)。**成立的三条**：①**最高优先级缺陷**=`store/strategy.go:1677` `BreakEvenStop{TriggerMode:profit_pct,TriggerValue:3}` 保本 arm 按**入场价百分比而非 ATR**计，`arm_ATR=trigger_pct/atr_pct` → 同一配置实际 arm 距离 **3.2× 离散**（实盘 610 单 atr_pct p10=0.558/p90=1.797），默认 3% 下中位币需 **2.94 ATR** 顺向才 arm，而亏损单平均只走到 **0.56 ATR** → 给唯一有效机制注入纯噪声；`BreakEvenStporRule.TriggerUnit` 已支持 `atr`，属配置项非新逻辑。②**arm 前移 0.60→0.30 ATR 两总体同号**：20 季原始 ATR 单位 +0.0348~+0.0407（19–20/20 季，p=0.0000，四种 stop 宽度全单调）＋实盘 539 单重放 +0.0979（bootstrap 95%CI[+0.0186,+0.1801] 不含 0）；机制=亏损单 −1.0639→−0.4813 ATR（**砍 55%**）、盈利单 +0.5164→+0.3482（砍 33%），亏损缩得更快 → 账本 −0.0436 **翻正 +0.0543**；`never arm` −0.4618 印证保护栈是账本能活的原因。**但必须同记反面**：按实际暴露风险归一后 arm 0.30 **更差**(−0.1419 vs −0.0954，只暴露 0.508 vs 1.201 ATR)=**靠少担风险赢不靠选得准**；研究总体是随机入场**负**期望（任何减暴露都显得好）、实盘是**正**期望（减暴露减利润），两者可反号，实盘重放同号才是能用的依据，上线前须按名义风险重标仓位。③目标超 2.0 ATR 无期望增量(1.0→2.0 p=0.0059，之后 p=0.25–0.59)却让季度 σ 0.0150→0.0462，封顶代价 −0.0078R **不显著(p=0.41)** 换 **6.42× 方差**下降，而实盘 `MaxTargetATRMul=5.0` 正处高方差端。**止损加宽是摩擦工程非预测**：净 R 单调至 ~4 ATR 但 **87% 收益来自摩擦份额**、gross 增量单独不显著(p=0.2517)、8 ATR 处 77% 是超时退出，且实盘止损中位已 2.44 ATR（`full_sl` 4.17）本不在陡峭段。**实盘诊断**：亏损单逆向 2.60/顺向 0.56 ATR vs 盈利单 0.90/2.21，胜率 62.1% 中位 +37.50bps，`break_even_stop` 215 单×+62bps 按设计工作，**48.7% 亏损单从未走到 0.5 ATR 顺向**保本永远 arm 不上 → 一路跑到 `structural_sl`；但此赢/亏分野是**事后**的、`probation2.py` 已证不可提前预测。预注册判据 `evalgate.py::PASS_RULE` 出结果前写死，主统计量=季度等权+二项符号检验，pooled p 不作决策。**本轮自我订正 4 次**（ADX/ATR 归零、K 冗余、probation 同义反复、arm 网格追分母）。数据 20 季/55 对，事件表 347499、多目标 1996260、不利标签 694998、OOS 542014。**第八条候选：震荡区间极限位置门禁（2026-08-04，用户依 CLUSDT id1898 与 SKHYNIX id1926 两笔亏损提出，已否决拦截，代码零改动）**——`research/range-extreme-gate/`，n=1759，几何**从当时 prompt 原文解析**（prompt 即因果记录，规避 kline 重对齐 70.7% 保真上限）。核心变量 `opp_atr`=入场到最近**逆向**结构区 ATR 距离（LONG→上方阻力/SHORT→下方支撑；方向未签名的"最近区"会把 CLUSDT 2.44x 与 SKHYNIX 0.44x 打成同一分）。预登记 5 判据（`CRITERIA.md`）全灭：AUC **0.514** 而安慰剂保护侧 `prot_atr` **0.502**（两侧同等无信息）；分箱中位数 +0.314/+0.343/+0.293/+0.247/+0.263 平坦无梯度；时间半分 tight−wide −0.088/−0.091 **与假设反号**；缩仓分支风险重归一化后 `opp_atr<0.5` 单位风险均值 **+0.001** 非负。**方向反转是最重要发现**：按方向签名 `adverse_pos>=0.75`（多头在顶/空头在底＝用户说的"极限位置"）n=893 中位 **+0.414/胜率 60.5%**，区间中部 n=830 只有 +0.207/54.8% —— 因为系统本质趋势跟随（LONG 963 笔 437 在上部、SHORT 796 笔 456 在下部），**"极限位置"是系统常态工作区也是最赚钱区，拦掉等于拦主收益**；1926 所属组合（逆向A级<0.5x＋逆势极限）n=120 中位 +0.430/60.0%，是 60% 胜率总体里的正常亏损。**缩仓需求早已满足**：`riskBasedPositionSize` 按 `effStop` 反算，SKHYNIX AI 要 100→实际 **32.19 USDT(0.32×，比用户要的 1/3 更狠)**、CLUSDT 300→196.02(0.65×)。**CLUSDT 真实缺陷与本门禁无关**：输在逆势（1h fib `direction=retracement_up`）＋止损 1.05→2.81 ATR 被 `structural` 档（`stop_loss_pct=3.2`/`backstop_atr_mul=3.1`）放宽，但那是**配置内设计行为**且仓位已随之缩小。**用户要的检查其实已存在但关闭**：`structural_blocking_level_too_close` 已实现，两实盘策略 `structural_min_blocking_pct` 均为 **0**，本轮数据说明保持 0 是对的。**三个保真自纪**：①因果 join 反了一整周期（决策记录在开仓**后** 10–12s 落库、周期 ~20min，"取入场前最后一条"必取上一周期，手工核对单被算成 0.752/2.300 而非 0.438/2.500）→ 改取 [入场−60s,+300s] 内第一条并把 **join 本身写进 selftest**（只 pin 解析器不 pin join 等于没 pin）；②`atr_price` 由 prompt "x.x ATR" 反解而该值**仅一位小数**，距离越近放大越狠 → 冒出 0.00%–0.2% 的不可能 ATR 和一笔 **343.7 ATR 假交易**，单笔主导整个分箱均值（+0.532，截尾后仅 +0.034）→ 改为只用 >=0.3x 的区反解取**中位数**、ATR% 越界(0.15–20%)弃样本；③相关子查询逐笔扫全部大 prompt（1922×~20k）两次 600s 超时零输出 → 两阶段（先建 (trader,created_at,id) 索引再按主键取 prompt）降到 **13.5s** |
| `loss-optimization-plan.md` | 亏损优化方案 A（风险预算定仓,已实现未部署）：size=(equity×pct)/止损距离,取 max(AI 声明,配置结构)止损；6 用例过,待用户定部署参数 | 未部署 |
| `coin-direction-vendor-eval.md` | 厂商方向模型交付物评估+二次开发（**第三轮(2026-07-29)整体横评裁决:作为「单币方向门禁」项目失败,不建议再投资源** —— 在 15 个诚实 walk-forward 季(251507 行/55 币/2023-01~2026-07)上判厂商**自己的** `state_from_probabilities`:合并净选币技能 +2.75bps p=0.0000 **但这是合并假象**,**按季等权均值 −6.94bps、中位 +0.00、t 检验 p=0.5959、8/15 正(二项 p=1.0000 掷硬币)**,最差季 −179.42bps,**6/15 季整季只出一个方向**(另 10/15 季 >90% 时点所有信号同号)→ **它是 beta/regime 表达器不是选币器**,我们表达 beta 有更便宜的方式;**必须记住的陷阱**:重铸件在 2026-07 真 OOS 打出 `+492.75bps p=0.0000` 是**假的** —— 该窗市场自身 −277bps/上涨占比 33.1%(下跌月),且重铸件 p_dir 上界 0.544 **低于 0.55 多头阈值** → 该季**只可能开空**(多头 0/空头 3585),**截面打乱对照**(同时点内打乱符号,保留多空比例与市场走势、只毁"哪个币")显示厂商件打乱后 +378bps **反而高于**实测 +325bps、净技能 **−52.93bps p=0.9195**,重铸件打乱=恒等变换净 +0.00bps;**硬过期 26-10-01 已解决且它不是缺陷**:V4/V6 读 artifact 自带 `valid_from`/`valid_until_exclusive` 超窗返回 `MODEL_OUT_OF_WINDOW`→NEUTRAL,这个设计是对的(季度模型静默续用更危险),真缺陷是交付物**没有铸造下一季的手段**(`fit_hurdle_model` 全交付零调用),已产品化为 **`research/coin-direction-eval/mint_artifact.py`(已进仓库)**:复刻配方(365 天窗/72h purge/30% 分位标签/cov>=0.80/按时间尾部 20% 做 Platt)输出厂商 joblib schema+带 sha256 的 manifest,**必须回读经厂商自己 runtime 打一次分**才算成功;2026Q4 今天铸不出(训练窗要到 2026-09-28 在未来)故改为**重铸 2026Q3 与厂商件对打**,17s 铸成、p_dir 排序 Spearman **+0.894** → 配方对;**订正**:trendable 门禁**不是**形同虚设(15 季拒绝 30.4%,2026Q3 的"100% trendable"是该季特例);**第 7 章正面结论仍成立但用途完全不同** —— 7.x 测的是"给**已开仓位**做单边减仓",不需要截面选币能力,只需对全市场方向有一点信息(正是 8.3 证明它有的 regime 味道),叠加腿部不对称(**LONG 整体 −1.20bps vs SHORT −8.84bps**,我们入场门禁已滤掉坏多头故 LONG 腿 p=0.2587 全不显著,空头才是缺口 p=0.0043)=**互补性,它唯一的存在理由**,代价是单边减仓破坏对冲**波动率涨 3.8×**(0.345%→1.308%);**结论仍只进 SHADOW**(能续命≠该用),除 `mint_artifact.py` 外仓库零改动;**方法论教训**:合并 p 值必须配按季等权复核、任何 edge 必须配截面打乱对照、见 p=0.0000 先查是不是单向押注** / **2026-07-29，未上线，脚本已归档 `research/coin-direction-eval/`）：**第二轮把推荐用法从 M5 升级为「对称规则」**（看空减 LONG、看多减 SHORT，永不反向）——拆腿回归证明 lean 是**方向**读数非波动读数（多头腿 +0.271%/SD 与空头腿 −0.246%/SD 反向且都显著，两腿之和不显著），M5 只减 LONG 白扔了一半信息；面板净技巧 +0.0516% p=0.0040 比 M5 高 33% 且最差季小 3.4 倍，实盘 756 笔 +6.474bps/+60.12USDT **p=0.0352 显著**而 M5 +1.644bps p=0.2524 不显著，我方实盘信号主要在**空头腿**（分离 −79.42 p=0.0012，截尾更强/剔除极值后 p=0.0004/两交易所各自显著/剔前3品种仍显著，唯一弱点=4 周里 07-06 独扛）；**必须记住的方法论**：①基线是负期望时任何减仓都显示为正收益，必须配环形位移对照（保留自相关+覆盖+减仓总量只打乱时点）才能分离"择时"与"少交易"，实测机械收益只占 9% ②环形位移比块 bootstrap 功效高得多（同一效应块 bootstrap 边缘 CI vs 位移 p=0.0015），不矛盾、前者区间估计后者精确检验 ③两个脚本小时集不同就不可比（5205 vs 31273 会得出相反结论）；**协议敏感性诚实记录**：24 组合改善 100% 为正且范围窄（+0.0995~+0.1552%）但只 67% 显著、最差季 −0.30%，是"真实但小 + ~430 有效观测"的形态，不可宣称稳赢；**本地替代已被证伪**（想解 2026-10-01 硬过期）：逼近 lean 样本外 R²≤0.05、直接拟合市场级 ridge 9/15 季不显著，根因=`mean_i σ(w·x_i)` 依赖横截面**分布**而非均值且 Winsorizer 每季重拟合中位数，先求均值就丢了逐币分布信息 → **逐币结构真的在贡献信息，只是只在纵向聚合后可用**（横截面选币仍为零），故过期只能产品化自建的 `walkforward.py` 训练驱动而非换简单模型绕过；**仍只进 SHADOW 且仓库零改动**（无 Go 接入/无影子落库/训练驱动未版本化）；**单币方向门禁=失败**（横截面去均值后 DOWN 优势 -0.0209% CI 跨零；逐币 BA>50% 仅 16/55；池化 BA 49.37% 且 UP 召回 25% vs DOWN 召回 73% 是偏空非判别力；59% 小时是 0%↔100% 整体开关；机制=诚实交叉拟合 Platt 斜率 -0.09~-0.01 而出厂值 +0.426，斜率≈0 把横截面压成 3pp 窄带）；**市场级风险阻尼=通过**（同一分数按小时取横截面均值得 lean，联合回归 lean +0.512%/SD CI[+0.198,+0.850] 显著而朴素 24h 跌币占比不显著、corr 仅 0.100，即 86 特征有朴素广度之外的增量；推荐 **M5 只减仓不反向** 倍率0.5/历史p50/lag1h：13 季 10 胜、最差季仅 -0.106%、小时级 +0.1018% CI[+0.042,+0.162]，选它是因 CI 最窄且永不反转交易员方向；倍率/阈值/成本扫描全显著，**lag 0~12h 无衰减→排除 V4 +1h 时间戳泄漏**；我方 slope168 占比同口径只 6/13 季，厂商读数更好）；**实盘 436 笔 LONG：+2.851bps/笔 +29.68USDT，逐周 3/4 正，但 p=0.22 不显著（4 个周块无功效），主证据是 13 季面板**；两条接口契约=V4 必须用收盘时间(开盘+1h,只加一次)、训练掩码必须用推理侧 cov>=0.80 而非全列有限；**必须按名义归一到 bps**，USDT 口径混合 4016 美元与 0.41 美元仓位测的是仓位噪声（归一后模型对朴素读数的优势基本消失）；**交付缺口**：无任何训练驱动、artifact 硬过期 2026-10-01 且要求季度重训 | 活跃 |
| `position-mgmt-gate.md` | 自研双向持仓管理门禁 PMG(2026-07-29,**研究完成、未上线、仓库零接入**,脚本 `research/position-mgmt-gate/` 1489 行):用户要「基于高胜率目的重新开发类 M5 双向管理门禁,M5 只参考不使用,开发单独基线并循环测试选拔变种」;**结论:纯路径式持仓管理是零和重分配** —— 高胜率目标可达且极稳(冠军 `tr0.25a1.0` Δ胜率 **+0.2913、18/18 季为正**、最差季 +0.2358、t p=0.000000,本项目测到过的最稳效应)**但期望与夏普一分不涨**(Δ期望 −9.24bps、**9/18 正、二项 p=1.0000**;波动 −49.2% 但夏普 −0.0048→**−0.0294**,成对 bootstrap Δ夏普>0 占比 **0.000**);机制是**卖右尾买胜率**(均盈 685→163、盈亏比 1.063→**0.265**、中位 −30→**+85**bps、99 分位 2839→767,而 1 分位 −2440→−1974 几乎没动);**同时满足胜率>基线且期望>=基线的变种 0/91**;**天花板已被直接测出**:取"持仓 12h 且浮亏>0.5ATR"的 74171 个决策时刻,用决策时可得的 8 个本地特征预测剩余收益,**验收 AUC=0.4999 vs 打乱零假设 0.4999、p=0.5050、五分位无单调性** → **决策时刻信息量为 0,天花板是结构性的不是网格不够大**;**一次自我否证**:条件化测试里 `sl3.0` 在选中层 Δ期望 +16.3bps Δ夏普 +0.0190 **p=0.0000**(空头腿 +23.6bps)看似强,**按季等权立刻塌掉 10/18 正、二项 p=0.8145** —— 与厂商那轮同一个合并假象,规矩是我自己定的这次挡住了自己;**顺带修了工具链两个 bug**:`harness.load_symbol` 因 `"open_time" not in df.columns` 静默丢弃 **154 个无表头 CSV**(其中 **25 个落在分析窗口内**:ICPUSDT 2022-01..06 连续 6 月、20 币的 2022-03 含 SOL),**中段缺口比起点晚危险得多**(slope168/ATR 跨缺口计算且无 NaN 标记),新 `bars.py` 逐文件嗅探表头 + `gap`/`contiguous_run` 作废跨缺口滚动值(全库真实缺口 20/55 币共 40 处,除 LITUSDT 2025-12 外全在 2022 故不影响 15 季结论);**方法论**:胜率必须配**硬下限**不能加权求和(加权会让高胜率把期望亏损"买"回来),自测里放"95% 胜率/负期望"假变种当哨兵;向量化模拟器 **1262×** 加速但**必须逐笔对齐标量版**(首轮 7 笔不一致,根因是漏了标量版 `if j > last: j = last` 的成交列夹取,修后偏差 1.39e-17/0 笔);**下一轮唯一值得做的**:找**不在单币价格路径里**的决策时刻输入(订单簿/资金费/持仓量/跨币溢出),**先过第 8 节那个 20 秒的 AUC 门槛再进模拟器**；**三类外生输入已逐个过筛，全部未过（2026-07-29，提交 `9118554`）**：跨币溢出 ΔAUC **+0.0009**（打乱 p=0.4650）、资金费率 **−0.0005**（p=0.8800）、持仓量/多空比 **+0.0004**（分位差 p=0.2754），三者全落在第 8 节天花板 AUC 0.4999 的噪声内，**订单簿因无历史快照未测**（需从现在起攒数据，是数据工程决定不是研究决定）；**两个「看起来显著」的陷阱形态务必记住**：① 资金费分位差 −33.6bps **p=0.0105 但符号相反**（模型判定最该恢复的一档表现最差）**且非单调**（Spearman −0.400）→ 单桶离群不是信号，*分位差显著必须同时看符号与单调性*；② OI 打乱 **p=0.0250 看似过关，但打乱标签会同时摧毁本地基线**（基线 AUC 已 0.5069），该 p 值量的是**本地特征而非候选** → *打乱零假设检验的是「整个模型 vs 噪声」而非「候选 vs 基线」，判据必须取 AND，且候选须战胜本地基线 AUC 而不是 0.5*；判据在**看结果之前**写死在 `screen.py`，OI 覆盖只从 2024-06-01 起故切分点前移至覆盖中点 2025-07-01（亦事前定好，`screen.run` 增 `split` 参）；**第 8 节那个 20 秒筛子连筛掉了三轮完整建模** | 已定论(研究完成,未上线) |
| `entry-gate-ai-selfreport-audit.md` | **入场门禁「只信 AI 自报、不验证」缺口审计 + 回测(2026-07-31,已回测,结论=不要修)**。**⚠️ 回测否掉了原提议的修法**:样本 n=8682(05-05~07-29,四交易员,24h完整后续K线,剔 2 笔价格错配脏数据 SPCXUSDT 0.7194→156.34 / XAUUSDT 68.9→4154)。**按真实止损重算 RR 会主动损害绩效**,且随持仓时长单调加强(真实持仓中位 4.66h/P75 11.81h/P90 18h,故必须测到 24h):真实RR≥1.5 放行组 6h -0.168%/胜率42.0% vs 拦截组 +0.075%/51.8%(差 -0.243,p=0.0000)→ 12h 差 -0.920 → **24h 差 -1.247,p=0.0000**。**机制三重确认**:(1) `rho(真实RR, 目标距离ATR)=+0.9988` 几乎恒等 —— 95.8% 样本自报风险低于 1.5ATR 地板,`real_risk` 被钉成常数 1.5×ATR,故**真实RR 退化成「目标距离÷1.5ATR」,不再是盈亏比而是「目标有多远」**;(2) 目标距离单独复现全部负效应([0,1)ATR 胜率 67.8% → [5,∞)ATR 26.6% 单调);(3) **自然实验**:floor 不生效的子样本(自报风险已≥1.5ATR,n=367)效应**消失**(差+0.099,p=0.3525),floor 生效子样本 -1.305,p=0.0000。**⚠️ 陷阱记录(教训值得记)**:中途曾得出「目标距离<1ATR」样本外增量 +1.1401、p=0.0000、四交易员三个月全正、样本外强于样本内 —— 漂亮得可疑,一查是**循环论证**:目标距离在模拟里同时决定止盈位,近目标容易被打到记小赢、远目标先吃 1.5ATR 止损;**拆掉止盈后信号完全消失(截尾期望差 -0.0198,p=0.8113)**,完全中性口径 p=0.6472;放行组胜率 67.8% 但均赢 +1.282%/均输 -3.326%,**期望值 -0.1996 比拦截组 -0.0518 更差** —— **中位数在不对称赔付下会说谎,胜率高不等于期望正**。**更根本的发现**:三种 RR 定义与结果的秩相关**全部接近零**(自报RR +0.0010/-0.0121、真实RR -0.0086/-0.0585、自报止损ATR倍数 -0.0106/-0.0488),现行 `自报RR>=1.5` 门禁本身也无预测力(中性中位差 -0.1732 p=0.0680,方向还是反的) → **真正结论不是「RR 算错了要修」而是「RR 这个判据本身价值很低」**;原审计的「风险低估 3~4 倍」在**风险度量**意义上成立(地面真相 `protection_plan_snapshots.tiers_json` n=293:claude 0.770%→2.901% / GPT 0.886%→3.525% / Claude-R 0.563%→1.636% / BN 0.767%→3.317%,自报止损中位 0.722ATR、95.9% 低于 1.5ATR floor),但**接进门禁做拦截没有收益甚至有害**;可做的是**审计形态记录真实RR与自报RR之差**(不拦截)用于观察 AI 是否系统性低报风险、并给仓位大小提供输入。48 项检查粗分类:仅读 AI 自报 14 项、仅实算 5 项、混合 17 项(含 HH/HL 两项结构检查,是该阶段**唯一**从 K 线实算摆动结构的)、其他 12 项。脚本 `rr_bt1.py`~`rr_bt9.py`:用户问「系统只接受AI分析而不验证」是否堵上了 —— **没有,只堵了 HH/HL 方向那一处**。**最大缺口:`rr_below_min` 用 AI 自报失效位算 RR,而实际执行的结构止损有 `floor_atr_mul=1.5` 硬下限**。**地面真相**(`protection_plan_snapshots.tiers_json` 落库的 triggerPrice,n=293,非推算)对比 7 月决策记录(n=6423)自报失效位:claude 0.770%→**实际2.901%(3.8×)** / GPT 0.886%→**3.525%(4.0×)** / Claude-R 0.563%→1.636%(2.9×) / BN 0.767%→3.317%(4.3×);自报止损换算 ATR14(1h) 中位仅 **0.786 ATR**、**95.5% 低于 1.5ATR floor** → floor 几乎永远在外推止损。**后果:min_rr=1.5 通过率 93.7% → 按真实止损重算只剩 24.5%**(min_rr=2.0 是 48.4%→10.5%),即当前 RR 门禁放行的单子约**七成**按真实止损不满足 min_rr,风险低估约 3~4 倍。`min_sl_distance_atr_mul=0.8` 不解决此问题(0.9ATR 能过 0.8 门禁但真实止损仍 1.5ATR)。**这不是新发现**:`production-deployment.md` 2026-07-20 结尾已写明「recompute RR against ENFORCED stop instead of AI nominal invalidation (SPCX class)」,**记下来一年没做**。48 项检查粗分类:**仅读 AI 自报 14 项**(rr_below_min/net_rr_below_min/net_rr_above_max/fake_retest_trap/range_middle_without_edge_setup/unsupported_setup_type/breakout_retest_blocked 等)、仅实算 5 项、混合 17 项(含本次新增的两项结构检查,是该阶段**唯一**从 K 线实算摆动结构的)、其他 12 项。**建议修法**:`effective_risk = max(入场价与AI失效位之差的绝对值, floor_atr_mul×ATR)` 再算 RR;**但影响面极大(通过率 93.7%→24.5%),必须先回测,不可直接上线**,否则等于把开仓量砍到四分之一;可先以 audit 形态记录真实RR与自报RR之差。 | 已回测,结论=不要修 |
| `entry-structure-gate-backtest.md` | 入场侧 HH/HL 结构闸门(**2026-07-31 已上线 claude-ct30+GPT-ct50 强制模式 pid 345250,lb=3/n=2/pct=0**)。**⚠️ 上线当天发现并修复的窗口缺陷,比参数选择更要紧**:闸门原先读决策上下文的 K 线,而那份数据是**按 `indicators.klines.primary_count` 为 AI prompt 裁剪过的** —— prompt 该多长和闸门需要多长历史是两件不相干的事,被耦合在一起。线上实测 `bars=21`(GPT-ct50 `primary_count=22` 去掉未收盘一根)。**按线上真实窗口截断重跑回测:21 根 → 放行率 7.7%、增量 +0.160、p=0.1522,即闸门在 GPT-ct50 上等于无效**,且 **92.3% 的拦截是「凑不齐 n=2 高+n=2 低」= 结构「看不见」而非「不存在」**,纯数据饥饿假象;claude-ct30(29 根)反而正常(23.7%/+0.309/p=0.0002);全历史 33.6%/+0.263/p=0.0004;40 根 32.6%/+0.276/p=0.0004。**修复**:新增 `structuralBars()`,上下文 < `structuralMinBars=30` 根时自取 120 根(`market.GetKlines`,60s `sync.Map` 缓存,键为 symbol+tf+exchange 三段拼接);先例是 `shadow_gate.go:143` 同类注释「PrimaryCount is far too short」。**两条硬约束**:(1) **取数失败必须弃权放行,绝不能当拦截** —— 这在执行路径上,取不到数不能变成交易决策;(2) **不照搬 shadow gate 的「取最长序列」兜底**,那会跨周期混用 15m 数据而结构效应在 15m 上**反向**,必须锁定 primary timeframe。`structuralMinBars=30` 不是摆动点存在的算术下限(那是 `2*lb+2=8`)而是**验证覆盖下限**:低于它就是在对回测从未覆盖的窗口执行拦截(8~29 根实测仅 15 笔占 0.5%,放行 1 笔,样本上无所谓但语义必须堵)。回归测试 `TestStructuralGate_ThinContextAbstainsInsteadOfBlocking` 复现该缺陷(同一笔逆势 LONG:全窗口拦截、21 根窗口弃权)。**结论按参数区分,不要笼统读**:`pct=0`(纯方向判据,无距离参数、无格点搜索=单一假设)**成立**;`pct=0.05` 等小数级调参格点**不该用**——它比 pct=0 好的那点(7月真实PnL +62.97 vs +62.85,121 笔)毫无意义,却引入「参数是调出来的」这个真实弱点。**严格样本外(按时间前后切两半,先定判据后切样本)**:lb=3 n=2 pct=0 样本内增量 +0.268 / **样本外 +0.272(n=535,p=0.0012)**;lb=3 n=3 pct=0 样本内 +0.289 / **样本外 +0.321(n=225,p=0.0072)**。样本外≈样本内是参数稳定的标志。**推荐 lb=3 / n=2 / pct=0**:11.5 笔/天(只砍一半频率,不是八成)、**28 个币种**(ZEC 27.0%/TON 23.7%/HYPE 17.5%)、逐币留一最不利 +0.180(基线 -0.034)、三个月增量全正(+0.283/+0.116/+0.326)、**7月真实PnL 四个 trader 全部改善**(GPT +20.17 vs 全体 +2.52 / claude +34.17 vs -14.16 / Claude-R -9.55 vs -20.25 / BN -12.38 vs -46.26,两个本身亏钱的是亏得更少)。备选 n=3 pct=0:4.5 笔/天、TON 占 55%、样本外 p=0.0072、7月真实PnL p=0.0192 显著(n=2 在真实PnL上 p=0.1442 不显著,方向与量级对)。**⚠️ 我在 2026-07-30 会话末尾曾把整个结论否掉,那次是往悲观方向漂移,已纠正,教训值得记**:(a) 我写「残余缺陷不可界定」去否定结论,但那 119 笔缺陷类拦截(`stop_inside_invalidation` 等)**入场价全部为空、从来没进过严格样本**,剔与不剔三个格点数字完全相同 —— 我在给一个**对结果零影响**的操作写反对意见;那 65% 虚增发生在早期用「最后一根K线收盘价」回填入场价的废弃版本上。(b) 我写「没有任何样本外证据」是**错的**,`tune1`/`tune8` 的时间切分就是样本外且 p=0.0012。(c) 多重比较与「真实PnL不是样本外」两条**只适用于小数级扫参**,对 pct=0 不适用(单一假设)。(d) 我写「频率降85%是机制本身(样本变小→误差棒变宽→均值好看)」,被 n=2 打掉:11.5 笔/天只砍一半频率,样本外增量仍 +0.272。**教训:发现自己此前有过度乐观漂移后,纠正时要逐条验证反对意见是否成立,否则会用同样不严谨的方式漂到另一侧;「剔除了某类缺陷」这种操作必须先验证它是否真的改变了样本。****仍然成立、不撤的弱点**:只有 3 个月历史、**无多年回测**(K线只能从 `decision_records.input_prompt` 挖,Binance 地理封锁、DB 无 kline 表);**`market_state` 那层仍无对照**(带入场价的 1144 条拦截**全部**来自 `structural_fit` 阶段,`market_state` 是全样本共同前提);`CorrelatedAdverseThrottle` 有 63 天真样本外为正仍因多年回测失败而默认关,本闸门样本外证据**类型相当但历史短得多**。**方法论(后续回测都该照做)**:已开仓单有真实PnL、被拦单没有,故两组都用**同一中性规则**重模拟(持有6h、无止损止盈)才可比;被拦单入场价**只用日志给出的**(用最后一根K线收盘价回填会系统性高估 +1.548%→+0.744%);**入场闸门拦截只进日志(留18天)不入库(持仓3个月)**,`execution_log` 只有执行阶段失败195条 —— 这个数据缺口是本次一切口径困难的根因。**口径陷阱:必须用 `realized_pnl`,全体价格变动%合计 -532% 而实盈 +41.16(仓位大小承担主要作用)**;既有闸门放行集 -0.040% vs 无闸门 -0.034% 这类**六千分之一个百分点的差不能当结论**(但新闸门 +0.234 vs -0.034 是另一个量级)。**主周期敏感**:效应在 1h 成立、15m 反向、4h 不显著 → 只能给主周期 1h 的策略用(claude-ct30/GPT-ct50/BN 是 1h,**Claude15 是 15m,不可用**,这与逐trader拆分里 Claude-R 表现最差互相印证)。顺带查清用户三问:`chart_trend` **已对称**(`block_side` 在该分支是死参数)、「空单需下方结构位」**是我编的**(已撤回,数据 p=0.51)、多周期一致**从未实现方向校验**(严格三周期同向→全样本0笔通过)。WLD 07-28 止损仅0.98%且全程MFE +0.00%、4H低点0.328从未被触及而价格走出+7.23% —— **个案用户判断对但不可推广**(全体搬止损到结构位净差近零;我最初只取已亏损单做对比是反向选择偏差,已作废) / **2026-08-01 追加 15m 专项(Claude-R,369 笔,中性 6h,基线 -0.071%)**:同一闸门在 15m 上参数完全不同——**`n=2`(部署默认值)无效**,唯一稳定区是 `n=3`(lb=3..6 连续为正),`n=4` 在 lb=5 变号=过拟合,**最优 `lb=4/n=3/pct=0`**(唯一 3 折全正且每折跑赢该折基线);参数精度的正确问法是**相邻格点是否变号**而非小数步长(lb/n 是整数)。**但反面必须同记**:无任何格点显著,且 `INVERTED lb=3/n=3` t=+1.56 高于任何正向配置 → 这就是只上 audit_only 的原因。**chart_trend 在 15m 上 100 格全负**(最好 -0.015,线上配置 -0.197),根因 `dirSlope>0` 单项 -0.238(t=-2.28),Claude-R 15m 入场是**均值回归型**,短窗趋势滤波砍掉盈利尾部,11 条 shadow 规则亦全 ≤ 基线 → 确认不上。**2+3 不能同时 enforce**:方案3 放行集 64% 落在方案2 内(Jaccard 0.16),叠加只买到 ~18 笔独立交易却把吞吐砍到 0.78 笔/天(-91%),每折 n 仅 6/13/13。**`adx_weak` 硬编码 period=14 让最优组合无法表达** → 新增 `RegimeGateParams.ADXPeriod`(默认 14 保持既有行为),ADX(14) 各阈值全部平到有害而 **ADX(10)>=30 是唯一有效变体**。**两个曾推翻结论的错误**:①harness 窗口必须等于线上 120 根(`gate15mLiveWindow`),原先传完整历史使 `chartSwingAlign` 分母被大数定律压回 0.5,align 上限只有 0.535,我曾据此断言 `align_min=0.55` 恒不通过=**harness 假象**,修正后 max=0.788 放行率 19%;②**`chartTrendGate` 返回 `block` 不是 `allow`**(chart_trend_gate.go:107),放行须写 `!block`。**harness 保真度上界 71% 不是 100%**(最佳 offset 70.7%,错位一根 K 解释部分,~12 笔无法解释,交易所口径已排除)→ **所有 sweep 数字仅作相对排序,不可当绝对期望值**;harness 已提交 `5035861`(`GATE15M=1` 守卫) | 已上线(claude+GPT),窗口缺陷已修 |

---

## 2. 固定开发管理方法（docs/，必须服从）

| 文件 | 作用 |
|---|---|
| `docs/FUXI_WORKFLOW_CN.md` | **总工作流**：优先级原则、执行原则、风险控制、自治推进模式 |
| `docs/DURABLE_EXECUTION_WORKFLOW_CN.md` | 长任务续航（TaskFlow × Agentic Coding） |
| `docs/Git工作流规范.md` | 分支/提交规范（dev 主线、feat/hotfix、commit 格式） |
| `docs/DECISIONS.md` | 关键决策记录 |
| `docs/DEVLOG.md` | 开发日志（最近进展看尾部） |
| `docs/TODO.md` / `docs/RISKS.md` | 待办 / 风险登记 |
| `docs/PROJECT_MEMORY_ARCHIVE_CN.md` | 接管阶段记忆归档总表 |

治理文档族（按需查）：`PROJECT_OVERVIEW_CN` / `ARCHITECTURE_CN` / `MODULE_INDEX_CN` / `DATA_MODEL_RELATIONS_CN` / `SYSTEM_TRUST_BOUNDARY_CN`。

---

## 3. 当前在途工作（滚动更新，已完成即移除）

> 2026-06-24 会话：三交易员排查 + 修复

- **已完成代码（未部署）**：① entry_time 兜底(OKX createdTime) ② peak 持久化+重启恢复(新表 `peak_pnl_states`) ③ 防空响应批量误平守卫(`MarkOpenPositionsAbsentFromExchangeClosed`) ④ UI 净盈亏移右侧+红绿 ⑤ NovaI fallback key 已替换进 DB。
- **验证**：`go build` ✅、`go test ./store/ ./trader/` ✅（含 2 个新守卫测试）、前端 `tsc` ✅。前端 1 个失败单测(`Ladder planned levels`)是 baseline 既有，非本次引入。
- **待办**：等用户定部署方式后重启 `nofx-trading` 生效。注意容器 `/app/nofx`(12:12)是未知暂存二进制，安全做法=从源码重建。
- **第 3 项守卫策略**：缺失 ≥2 且=全部本地 OPEN 时判瞬时故障跳过；单个消失仍正常平。
